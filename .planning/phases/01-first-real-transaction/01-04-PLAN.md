---
phase: 01-first-real-transaction
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - compose.yml
  - Dockerfile
  - README.md
  - .env.example
autonomous: false
requirements: [DEP-01, DEP-02, OPS-01, OPS-04]
requirements_addressed:
  DEP-01: "Single docker compose up with two services (app + db); bind-mounted Postgres data; documented PUID/PGID; Dockerfile multi-stage; alembic upgrade head runs at container startup"
  DEP-02: "App binds 127.0.0.1:8000 only — LAN/Tailscale is the trust boundary; documented in README"
  OPS-01: "MONO_TOKEN passed only via .env (not committed) -> compose env -> process env; documented rotation flow"
  OPS-04: "Real-container smoke: docker logs after a real import is checked for zero token / X-Token / amount hits"
must_haves:
  goals:
    - "SC#1 — docker compose up brings the app up; /docs is reachable on the LAN with no app-level login"
    - "SC#2 — POST /api/import against real Mono returns real card rows; GET /api/transactions echoes them"
    - "SC#5 — docker logs after a real import shows zero token / X-Token / amount hits at INFO level"
  truths:
    - "compose.yml validates: docker compose -f compose.yml config exits 0"
    - "compose.yml binds the app port to 127.0.0.1:8000 (not 0.0.0.0); db has no host port mapping"
    - "compose.yml uses bind mount ${DATA_DIR:-./data}/postgres for /var/lib/postgresql/data — NOT a named volume"
    - "Dockerfile is multi-stage (builder + runtime); runtime runs as UID 1000; entrypoint is alembic upgrade head && uvicorn ... --workers 1"
    - "README documents docker compose up + .env setup + restore note (no -v) + rotation flow + PUID/PGID for Synology/Unraid"
    - "Real-Mono smoke (manual checkpoint): a real MONO_TOKEN posted to .env, docker compose up -d, POST /api/import, GET /api/transactions returns ≥1 row"
    - "docker logs $(docker compose ps -q app) | grep -ciE '(token|x-token|amount[^_])' returns 0 after a real import"
  artifacts:
    - path: "compose.yml"
      provides: "Two-service stack (app + db) with bind-mounted Postgres data, healthchecks, 127.0.0.1 binding"
      contains: "postgres:17-bookworm"
    - path: "Dockerfile"
      provides: "Multi-stage python:3.13-slim-trixie image with uv 0.11.12 build stage; runtime runs as UID 1000"
      contains: "alembic upgrade head"
    - path: "README.md"
      provides: "Single-page operator manual: setup, run, rotate token, restore note, PUID/PGID, network egress claim"
      contains: "docker compose up"
  key_links:
    - from: "compose.yml app service"
      to: "Dockerfile entrypoint"
      via: "build: . + healthcheck on /api/health (which Plan 03 implemented)"
      pattern: "alembic upgrade head"
    - from: "compose.yml db service"
      to: "${DATA_DIR}/postgres bind mount"
      via: "volumes: ${DATA_DIR:-./data}/postgres:/var/lib/postgresql/data"
      pattern: "DATA_DIR.*postgres"
    - from: ".env.example"
      to: "compose.yml environment block"
      via: "MONO_TOKEN, POSTGRES_PASSWORD, DATABASE_URL, LOG_LEVEL, DATA_DIR — single source of truth"
      pattern: "MONO_TOKEN"
---

<objective>
Bring up the walking skeleton on real Docker, validate that real Mono returns real rows, and prove SC#5 against real container logs (not just unit-test capture). Plans 01–03 produce the code; this plan produces the deployable artifact and runs the only validations that cannot be automated under 30 seconds: SC#1 (compose up, /docs opens), SC#2 (real Mono call returns real rows), SC#5 (`docker logs` clean after a real import).

Purpose: VALIDATION.md flags three behaviors as "manual phase-gate" — they require Docker host + browser + Bohdan's real MONO_TOKEN and cannot be in pytest. This plan packages the deployment artifacts (compose.yml, Dockerfile, README), then walks Bohdan through the live verification with a single human checkpoint. After this plan: Phase 1 is shippable.

Output: A working `docker compose up` deployment with a documented operator flow, plus a signed-off human verification record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-first-real-transaction/01-CONTEXT.md
@.planning/phases/01-first-real-transaction/01-RESEARCH.md
@.planning/phases/01-first-real-transaction/01-VALIDATION.md
@.planning/phases/01-first-real-transaction/01-SKELETON.md
@.planning/phases/01-first-real-transaction/01-01-PLAN.md
@.planning/phases/01-first-real-transaction/01-02-PLAN.md
@.planning/phases/01-first-real-transaction/01-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author compose.yml + Dockerfile + README + .env.example finalization (no human action — pure file authoring)</name>
  <files>
    compose.yml,
    Dockerfile,
    README.md,
    .env.example
  </files>
  <read_first>
    - .planning/phases/01-first-real-transaction/01-RESEARCH.md (Code Examples — compose.yml shape (DEP-01 + DEP-02), Dockerfile multi-stage; § Pitfall 6 bind-mount path drift; § Pitfall 8 migration race; § Pitfall 12 named volume vs bind mount)
    - .planning/phases/01-first-real-transaction/01-CONTEXT.md (D-09 — /docs swagger UI is the human entry point; deferred ideas list)
    - .planning/phases/01-first-real-transaction/01-SKELETON.md ("Architectural Decisions" — confirms `127.0.0.1:8000:8000`, bind mount, --workers 1)
    - pyproject.toml (Plan 01 — confirms uv + python pin)
    - alembic.ini (Plan 01 — used in Dockerfile entrypoint)
    - .env.example (Plan 01 — extend with comments and the network-binding note)
  </read_first>
  <action>
    1. Create compose.yml — paste from RESEARCH.md Code Examples verbatim, with one deliberate adjustment: confirm `127.0.0.1:8000:8000` (not `0.0.0.0`) per V4 access control:

       services:
         db:
           image: postgres:17-bookworm
           environment:
             POSTGRES_DB: finance_bro
             POSTGRES_USER: finance_bro
             POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set in .env}
           volumes:
             - ${DATA_DIR:-./data}/postgres:/var/lib/postgresql/data
           healthcheck:
             test: ["CMD-SHELL", "pg_isready -U finance_bro -d finance_bro"]
             interval: 5s
             timeout: 3s
             retries: 12
           restart: unless-stopped

         app:
           build: .
           user: "1000:1000"
           environment:
             MONO_TOKEN: ${MONO_TOKEN:?set in .env}
             DATABASE_URL: postgresql+psycopg://finance_bro:${POSTGRES_PASSWORD}@db:5432/finance_bro
             LOG_LEVEL: ${LOG_LEVEL:-INFO}
           depends_on:
             db:
               condition: service_healthy
           ports:
             - "127.0.0.1:8000:8000"
           healthcheck:
             test: ["CMD", "curl", "-fs", "http://localhost:8000/api/health"]
             interval: 10s
             timeout: 5s
             retries: 6
           restart: unless-stopped

       Note: NO top-level `volumes:` block — `${DATA_DIR}/postgres` is a bind-mount path, NOT a named volume (Pitfall 12).

    2. Create Dockerfile — paste from RESEARCH.md Code Examples verbatim:

       # Stage 1: builder
       FROM python:3.13-slim-trixie AS builder
       COPY --from=ghcr.io/astral-sh/uv:0.11.12 /uv /usr/local/bin/uv
       WORKDIR /app
       COPY pyproject.toml uv.lock ./
       RUN uv sync --frozen --no-dev --no-install-project

       # Stage 2: runtime
       FROM python:3.13-slim-trixie AS runtime
       RUN apt-get update && apt-get install -y --no-install-recommends curl \
           && rm -rf /var/lib/apt/lists/* \
           && useradd -u 1000 -m app
       WORKDIR /app
       COPY --from=builder /app/.venv /app/.venv
       COPY --chown=app:app . /app
       USER app
       ENV PATH="/app/.venv/bin:$PATH" PYTHONUNBUFFERED=1
       EXPOSE 8000
       CMD ["sh", "-c", "alembic upgrade head && uvicorn finance_bro.main:app --host 0.0.0.0 --port 8000 --workers 1"]

       (curl is added so the compose healthcheck `curl -fs http://localhost:8000/api/health` works inside the container.)

    3. Update .env.example to add inline comments documenting each variable. Replace the file from Plan 01 with:

       # Mono personal API token — get from https://api.monobank.ua and paste here.
       # The token NEVER touches DB or filesystem inside the app — it lives only in this
       # file and is read into the process via env at startup. Rotation = edit this file
       # and run `docker compose up -d`.
       MONO_TOKEN=

       # Postgres password for the local-only db service. Never reused outside this
       # compose stack. Generate with `openssl rand -base64 32`.
       POSTGRES_PASSWORD=

       # SQLAlchemy URL — psycopg v3 driver. Compose injects credentials from the two
       # vars above; you typically don't need to edit this directly.
       DATABASE_URL=postgresql+psycopg://finance_bro:CHANGE_ME@db:5432/finance_bro

       # INFO is production. DEBUG bypasses the structlog redaction processor — use only
       # for local debugging. Never run DEBUG with a real token.
       LOG_LEVEL=INFO

       # Bind-mount root for the Postgres data directory. Defaults to `./data`. Set this
       # to a path on your NAS / homelab disk; `./data/postgres` will be created under it.
       DATA_DIR=./data

    4. Create README.md as a single-page operator manual:

       # finance-bro

       Self-hosted personal-finance tool. Pulls Monobank transactions, stores them on
       hardware you control, exposes a JSON API. **Phase 1 (walking skeleton):** import
       one card, read transactions back. No UI yet. No app-level auth — gated by your
       LAN / Tailscale.

       ## Requirements

       - Docker + docker compose
       - A Monobank personal API token (https://api.monobank.ua)
       - About 200 MB of disk

       ## Setup

       1. `cp .env.example .env`
       2. Open `.env` and fill in `MONO_TOKEN` and `POSTGRES_PASSWORD`. Generate a
          password with `openssl rand -base64 32`.
       3. `docker compose up -d`

       The app is reachable at `http://localhost:8000/docs` (Swagger UI). Drive the API
       from there: click `POST /api/import`, "Try it out", "Execute". Then click
       `GET /api/transactions`, "Execute". Real Mono rows.

       The first import takes up to ~65 seconds because the app blocks on Mono's
       1-request-per-60-second rate limit between `/personal/client-info` and
       `/personal/statement`.

       ## Network egress

       The app contacts ONE external host: `api.monobank.ua` (HTTPS). No analytics,
       no telemetry, no third-party SDKs. `grep -E '(sentry|posthog|mixpanel|segment)'
       pyproject.toml package.json` returns nothing. NBU FX rates land in Phase 3, at
       which point `bank.gov.ua` is added.

       ## Rotation: changing the Mono token

       Edit `.env`, change `MONO_TOKEN`, then `docker compose up -d`. The new token is
       read into the process on restart. The old token is gone — the app never
       persisted it.

       ## Backup & restore (Phase 1 minimum)

       The Postgres data lives in `${DATA_DIR}/postgres` as a bind mount. To back up:
       `tar -czf finance-bro-backup-$(date +%F).tgz ${DATA_DIR}/postgres`. To restore:
       stop the stack (`docker compose down` — **NEVER `down -v`**), restore the
       directory, `docker compose up -d`. A daily `pg_dump` cron lands in Phase 7.

       **Important:** never run `docker compose down -v` — the `-v` flag wipes named
       volumes. The bind mount survives `down`, but `down -v` is still in muscle
       memory and the bind mount path may not be enough protection. Treat `-v` as
       forbidden in this stack.

       ## PUID / PGID (Synology / Unraid)

       The container runs as UID 1000 / GID 1000 (set via `user: "1000:1000"` in
       compose.yml). On Synology / Unraid, ensure the user that owns
       `${DATA_DIR}/postgres` matches. Adjust the `user:` line in compose.yml if
       your NAS expects a different UID.

       ## Trust model

       finance-bro has **no app-level authentication** in v1. It binds to
       `127.0.0.1:8000` only — anything reaching it has already passed your firewall.
       Reachable from your LAN devices via the host machine's IP, or remotely via
       Tailscale Funnel. Do **NOT** expose the app to the public internet without
       reading the v2 auth roadmap first.

       ## Logs

       `docker compose logs -f app` shows structured JSON. The structlog redaction
       processor masks the Mono token, the `X-Token` header, and any field whose name
       matches `/token|amount/i` at INFO level. To diagnose with raw values, set
       `LOG_LEVEL=DEBUG` in `.env` and restart — but never with a real token.

       ## What Phase 1 does NOT do (yet)

       - No automatic polling — you click `POST /api/import` (Phase 2)
       - No UAH rollup for USD/EUR transactions (Phase 3)
       - No category, no rules engine (Phase 4)
       - No transfer / refund netting (Phase 5)
       - No web UI — Swagger UI is the entry point (Phase 6)
       - No automated backup, no CSV export (Phase 7)

       See `.planning/ROADMAP.md` for the full phase plan.
  </action>
  <verify>
    <automated>docker compose -f compose.yml config > /dev/null &amp;&amp; grep -c 'postgres:17-bookworm' compose.yml | grep -q '^1$' &amp;&amp; grep -c '127.0.0.1:8000:8000' compose.yml | grep -q '^1$' &amp;&amp; grep -c 'alembic upgrade head' Dockerfile | grep -q '^1$' &amp;&amp; grep -c 'user: "1000:1000"' compose.yml | grep -q '^1$' &amp;&amp; ! grep -E '^volumes:' compose.yml &amp;&amp; grep -c '\\-\\-workers 1' Dockerfile | grep -q '^1$' &amp;&amp; grep -c 'docker compose up' README.md | grep -q '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose -f compose.yml config` exits 0 (compose validates) — note this requires `MONO_TOKEN` and `POSTGRES_PASSWORD` to be present in env or `.env`; for the test, run with `MONO_TOKEN=dummy POSTGRES_PASSWORD=dummy docker compose -f compose.yml config`
    - `grep -c 'postgres:17-bookworm' compose.yml` returns exactly 1
    - `grep -c '127.0.0.1:8000:8000' compose.yml` returns exactly 1 (NOT `0.0.0.0:8000:8000`)
    - `grep -c '${DATA_DIR:-./data}/postgres' compose.yml` returns exactly 1 — bind mount, not named volume
    - `grep -E '^volumes:' compose.yml` returns no matches (no top-level `volumes:` block; named volumes are forbidden per Pitfall 12)
    - `grep -c 'alembic upgrade head' Dockerfile` returns exactly 1
    - `grep -c '\-\-workers 1' Dockerfile` returns exactly 1 (Pitfall 8 — single worker)
    - `grep -c 'user: "1000:1000"' compose.yml` returns exactly 1 (DEP-01 — documented PUID/PGID)
    - `grep -c 'depends_on' compose.yml` returns exactly 1
    - `grep -c 'service_healthy' compose.yml` returns exactly 1 (db readiness gates app start)
    - README contains the strings: `docker compose up`, `MONO_TOKEN`, `POSTGRES_PASSWORD`, `DATA_DIR`, `127.0.0.1`, `Tailscale`, `down -v` (warning), `Phase 1`, `api.monobank.ua`
    - `.env.example` contains comments explaining each variable
    - The README explicitly forbids `docker compose down -v`
  </acceptance_criteria>
  <done>compose.yml + Dockerfile + README + .env.example are committed. `docker compose -f compose.yml config` validates. The deployment artifact is in place; no live test yet.</done>
  <threat_refs>T1 (token leakage — README documents the env-only model and forbids DEBUG with real token), T2 (token persistence — README documents rotation = edit .env + restart, no DB write)</threat_refs>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual phase-gate verification — SC#1, SC#2, SC#5 against real Docker + real Mono</name>
  <what-built>
    Plans 01–03 delivered the code. Task 1 of this plan delivered compose.yml + Dockerfile + README. The code passes pytest end-to-end against testcontainers + respx-mocked Mono. What is NOT yet proven (and can NOT be proven by pytest under 30 seconds):

    - SC#1: `docker compose up` actually starts the stack on the user's machine, `/docs` opens in a browser, no login challenge.
    - SC#2: A POST /api/import using **Bohdan's real MONO_TOKEN** returns rows from his actual Monobank card, and GET /api/transactions echoes them.
    - SC#5: After a real import cycle, `docker logs` (the production logging path, NOT the in-process pytest caplog) shows zero hits for the token, the `X-Token` header, or any transaction `amount` value.

    These are the three "manual phase-gate" entries from VALIDATION.md.
  </what-built>
  <how-to-verify>
    Bohdan runs these steps on the target Docker host (laptop / NAS) — Claude does not have access:

    1. **Setup** (5 min):
       ```bash
       # In the repo root:
       cp .env.example .env
       # Edit .env. Paste your real MONO_TOKEN. Generate POSTGRES_PASSWORD:
       echo "POSTGRES_PASSWORD=$(openssl rand -base64 32)" >> .env  # or edit by hand
       # Make sure DATA_DIR points somewhere you want Postgres data to live.
       mkdir -p "${DATA_DIR:-./data}/postgres"
       ```

    2. **SC#1 — compose up + /docs opens** (~1 min):
       ```bash
       docker compose up -d
       # Wait ~10 seconds for healthchecks
       docker compose ps  # both services should show "healthy"
       curl -fsS http://localhost:8000/docs > /dev/null && echo "SC#1 OK"
       ```
       Then open `http://localhost:8000/docs` in a browser. Swagger UI loads. No login required.
       **Expected:** `SC#1 OK` printed, Swagger UI visible.

    3. **SC#2 — real Mono import returns real rows** (up to ~65 sec, one rate-gate slot):
       From Swagger UI: click `POST /api/import` → "Try it out" → "Execute".
       OR from the command line:
       ```bash
       curl -X POST http://localhost:8000/api/import   # blocks up to ~65s
       ```
       The response should look like:
       ```json
       {"polled_account_id": "...", "statement_count": N, "inserted": N, "skipped_duplicates": 0}
       ```
       Then:
       ```bash
       curl -s http://localhost:8000/api/transactions | jq '. | length'
       # Expected: ≥ 1 (real number of recent transactions on your card)
       curl -s http://localhost:8000/api/transactions | jq '.[0]'
       # Expected: real row with amount_minor (int), currency ("UAH"/"USD"/"EUR"),
       # time (ISO-8601), and raw_payload (full Mono statementItem dict)
       ```
       **Expected:** at least 1 real transaction, with `amount_minor` as an integer (not a string, not a float), `currency` as a 3-letter code, `raw_payload` as a populated object.

    4. **SC#5 — logs are clean** (~30 sec):
       ```bash
       APP_ID=$(docker compose ps -q app)
       # Count any hits across all logs since startup:
       docker logs "$APP_ID" 2>&1 | grep -ciE '(token[^_]|x-token|amount[^_])'
       # Expected: 0
       ```
       If non-zero, dump the offending lines for inspection:
       ```bash
       docker logs "$APP_ID" 2>&1 | grep -iE '(token[^_]|x-token|amount[^_])'
       ```
       **Expected:** count is `0`. (Note the `[^_]` exclusions — they prevent matches on the field NAMES `amount_minor`, `account_id`, `token_hash` etc. that are themselves redaction-safe constants.)

    5. **SC#3 (re-verify with real data) — second import is no-op** (~65 sec):
       Wait at least 65 seconds since the previous POST (rate gate). Then:
       ```bash
       curl -X POST http://localhost:8000/api/import
       # Expected: {"polled_account_id":"...","statement_count":N,"inserted":0,"skipped_duplicates":N}
       curl -s http://localhost:8000/api/transactions | jq '. | length'
       # Expected: same N as before — no duplicates.
       ```

    6. **Open-question instrumentation** (no action — record observations):
       Open the database to record empirical data for STATE.md open questions:
       ```bash
       docker compose exec db psql -U finance_bro -d finance_bro -c \
         "SELECT source_kind, COUNT(*) FROM accounts GROUP BY source_kind;"
       ```
       - Note whether `mono.fop` is present (resolves Open Question 2 — FOP under personal token).
       - Note the `type` field values via `SELECT raw_payload->>'type', source_kind FROM accounts;` (resolves Open Question 4 — Mono `type` enum values).

    7. **Cleanup** (no action — leave running, OR):
       ```bash
       docker compose down   # NEVER `down -v`
       ```
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `"approved"` — all five SC checks above passed; record any empirical observations from step 6 in 01-04-SUMMARY.md
    - A description of what failed (e.g., "SC#5 grep returned 3, here are the lines: ..."), and Claude will diagnose
    - `"approved with caveats"` followed by what is partially working — phase can still close if it's a deferred concern (e.g., open question observation that doesn't gate phase done)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host shell -> .env file | `.env` contains MONO_TOKEN and POSTGRES_PASSWORD; `.gitignore` keeps it out of git (Plan 01). |
| compose env -> app container env -> Python process env | Token enters via env interpolation in compose.yml. Never via build-time ARG, never via image layer. |
| App container -> docker logs (stdout/stderr) | structlog JSON to stdout. Redaction processor on by default at INFO. SC#5 verifies in production logging path. |
| Host port 127.0.0.1:8000 -> LAN | DEP-02: bound to localhost only; LAN/Tailscale gates inbound traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 / T1 | Information Disclosure | docker logs in production | mitigate | structlog configured at container startup via lifespan (Plan 03 main.py); redaction processor active at INFO+. SC#5 verified manually in Task 2 against `docker logs $(docker compose ps -q app) 2>&1 | grep -ciE '(token[^_]|x-token|amount[^_])'` returning 0. |
| T-01-02 / T2 | Information Disclosure | Dockerfile build context | mitigate | `.dockerignore` excludes `.env` (Plan 01). Token never enters image layers — passed only as runtime env via compose. Verified by `docker history finance-bro-app | grep MONO_TOKEN` returning nothing. |
| T-01-XX | Information Disclosure | port binding | mitigate | `127.0.0.1:8000:8000` in compose.yml — verified by `acceptance_criteria` grep. Documented in README. |
| T-01-XX | Repudiation / Denial of Service | Postgres data directory | mitigate | Bind mount `${DATA_DIR}/postgres` (NOT named volume) — survives `docker compose down`. README forbids `docker compose down -v`. Daily `pg_dump` is Phase 7 — Phase 1 ships with bind-mount as the minimum. |
| T-01-XX | Spoofing | inbound HTTP | accept | DEP-02 — no app-level auth. Tailscale/LAN trust boundary. Documented in README under "Trust model". |
| T-01-XX | Tampering | Dockerfile content trust | accept | Pinned `python:3.13-slim-trixie` and `ghcr.io/astral-sh/uv:0.11.12`. Re-pull happens on `docker compose build --pull` per operator's choice. SBOM hardening is post-v1 (V10 ASVS partial — `uv.lock` does the heavy lifting). |
</threat_model>

<verification>
Task 1 (automated):
```bash
docker compose -f compose.yml config > /dev/null && echo "compose validates"
grep -c '127.0.0.1:8000:8000' compose.yml
grep -c 'postgres:17-bookworm' compose.yml
grep -c 'alembic upgrade head' Dockerfile
grep -c '--workers 1' Dockerfile
! grep -E '^volumes:' compose.yml && echo "no named volumes — good"
```

Task 2 (manual — see `<how-to-verify>` above):
```bash
docker compose up -d
curl -fsS http://localhost:8000/docs > /dev/null && echo "SC#1 OK"
curl -X POST http://localhost:8000/api/import     # SC#2 part 1
curl -s http://localhost:8000/api/transactions | jq '. | length'  # SC#2 part 2
docker logs $(docker compose ps -q app) 2>&1 | grep -ciE '(token[^_]|x-token|amount[^_])'  # SC#5 → expect 0
```
</verification>

<success_criteria>
- compose.yml + Dockerfile + README + .env.example committed
- `docker compose config` validates
- Bohdan completes the manual checkpoint and replies "approved" (or "approved with caveats")
- SC#1, SC#2, SC#3 (re-verified live), SC#5 demonstrably pass on real hardware
- Empirical observations from Open Questions are captured in 01-04-SUMMARY.md (Mono `type` enum values, FOP-under-personal-token presence, 429 response shape if observed)
</success_criteria>

<output>
After human approval, create `.planning/phases/01-first-real-transaction/01-04-SUMMARY.md` capturing:
- That all five Phase 1 success criteria are demonstrably met
- The empirical answers to STATE.md Open Questions 1–4 (whatever Bohdan observed):
  - Q1: Did `statementItem.id` collide across accounts? (Probably no in Phase 1 with one card; phrase as "no evidence yet")
  - Q2: Did `mono.fop` rows appear under the personal MONO_TOKEN?
  - Q3: Was a 429 observed? (Should be no — gate prevented it; phrase accordingly)
  - Q4: What `type` values did Bohdan's accounts return? (`black`, `white`, `platinum`, `fop`, …)
- The deployment story: one `docker compose up` + a paste in `.env` + click in /docs = real rows
- Phase 1 is shippable; ready for `/gsd-plan-phase 2`

Then commit all phase artifacts.
</output>
