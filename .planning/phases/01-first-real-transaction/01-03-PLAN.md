---
phase: 01-first-real-transaction
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/finance_bro/db/account_repo.py
  - src/finance_bro/db/transaction_repo.py
  - src/finance_bro/services/__init__.py
  - src/finance_bro/services/import_service.py
  - src/finance_bro/api/__init__.py
  - src/finance_bro/api/schemas.py
  - src/finance_bro/api/deps.py
  - src/finance_bro/api/routes_health.py
  - src/finance_bro/api/routes_accounts.py
  - src/finance_bro/api/routes_transactions.py
  - src/finance_bro/api/routes_import.py
  - src/finance_bro/main.py
  - tests/test_health.py
  - tests/test_idempotency.py
  - tests/test_import_route.py
  - tests/test_transactions_route.py
autonomous: true
requirements: [ING-01, ING-03, ING-04, ING-07, FX-01, OPS-01, OPS-04, DEP-02]
requirements_addressed:
  ING-01: "POST /api/import drives MonobankImporter.discover_accounts (lazy, first call only) and fetch_statement for the first card (D-04)"
  ING-03: "Accounts and transactions persisted in Postgres with full Mono statementItem retained as raw_payload JSONB"
  ING-04: "INSERT ON CONFLICT (account_id, source_tx_id) DO NOTHING on partial unique index; response.skipped_duplicates surfaces the count"
  ING-07: "Soft-delete column unread by Phase 1 code paths; API never sets is_deleted=true; test asserts immutability of raw_payload"
  FX-01: "API exposes amount_minor as JSON int (not string, not float); currency as ISO-4217 alpha string"
  OPS-01: "Import service reads MONO_TOKEN via Settings (env-only); never persists or logs it"
  OPS-04: "Full import cycle test asserts zero token / X-Token / amount substrings reach INFO log records"
  DEP-02: "All routes unauthenticated; /docs reachable; tests confirm no auth dependency"
must_haves:
  goals:
    - "SC#1 — Bohdan starts the app via docker compose up, opens /docs without login, and the API is reachable"
    - "SC#2 — POST /api/import polls one card and writes rows; GET /api/transactions returns those rows with amount_minor (BIGINT signed minor units), currency (ISO-4217 alpha), and verbatim raw_payload (D-10)"
    - "SC#3 — Two POST /api/import in quick succession do NOT create duplicate rows; second call returns inserted=0, skipped_duplicates=N"
    - "SC#5 (partial) — full import cycle leaves zero token / X-Token / amount values in INFO log capture"
  truths:
    - "POST /api/import is synchronous, takes no body, and returns {polled_account_id, statement_count, inserted, skipped_duplicates} (D-02 — manual trigger only, no scheduler in Phase 1; D-08 — response shape)"
    - "First POST /api/import discovers accounts via /personal/client-info, then proceeds to /personal/statement/... after the gate releases; app startup is silent — no rate budget burned before the user asks (D-03 — lazy validation; D-06 — one-shot discovery, subsequent imports read accounts from DB)"
    - "All accounts Mono returns (cards + jars + FOPs) are persisted on first import (D-05); only the first mono.card is polled (D-04)"
    - "GET /api/transactions returns rows ordered by time DESC; no pagination, no filtering (D-07)"
    - "GET /api/health returns {status: ok, db: ok} when SELECT 1 succeeds; used by compose healthcheck (D-09)"
    - "GET /api/accounts returns the discovered account list (D-09)"
    - "Repeated imports of the same Mono statementItem.id under the same account_id produce zero duplicate rows because of ON CONFLICT DO NOTHING on the partial unique index from migration 0001"
    - "raw_payload column round-trips verbatim: GET /api/transactions response[i].raw_payload equals the original Mono statementItem dict"
    - "Pydantic response model declares amount_minor as int; basedpyright strict passes"
  artifacts:
    - path: "src/finance_bro/services/import_service.py"
      provides: "ImportService.run_one_card(now=None) -> ImportResult — the orchestrator that wires Mono adapter + repos"
      contains: "ON CONFLICT"
    - path: "src/finance_bro/db/account_repo.py"
      provides: "AccountRepo.list_all, .get_first_card, .upsert_many"
      contains: "class AccountRepo"
    - path: "src/finance_bro/db/transaction_repo.py"
      provides: "TransactionRepo.insert_many (ON CONFLICT DO NOTHING) + .list_for_account"
      contains: "on_conflict_do_nothing"
    - path: "src/finance_bro/api/routes_import.py"
      provides: "POST /api/import handler — synchronous, calls ImportService.run_one_card"
      contains: "router.post"
    - path: "src/finance_bro/api/routes_transactions.py"
      provides: "GET /api/transactions handler — returns flat list ordered by time DESC"
      contains: "router.get"
    - path: "src/finance_bro/api/schemas.py"
      provides: "Pydantic response models (TransactionOut, AccountOut, ImportResultOut, HealthOut)"
      contains: "amount_minor: int"
    - path: "src/finance_bro/main.py"
      provides: "FastAPI app with all four routers wired in lifespan; structlog configured"
      contains: "include_router"
  key_links:
    - from: "src/finance_bro/api/routes_import.py"
      to: "src/finance_bro/services/import_service.py"
      via: "FastAPI dependency Depends(get_import_service)"
      pattern: "Depends\\(get_import_service\\)"
    - from: "src/finance_bro/services/import_service.py"
      to: "src/finance_bro/importers/monobank.py + rate_limit.py"
      via: "MonobankImporter(token, RateLimitGate(session_factory)).discover_accounts() / fetch_statement()"
      pattern: "MonobankImporter\\("
    - from: "src/finance_bro/db/transaction_repo.py"
      to: "alembic/versions/0001_walking_skeleton.py (uq_transactions_account_source_tx)"
      via: "INSERT ... ON CONFLICT (account_id, source_tx_id) WHERE NOT is_deleted DO NOTHING"
      pattern: "on_conflict_do_nothing"
---

<objective>
The walking-skeleton vertical slice. After this plan, a real user can: (1) start the stack with the app already wired (compose lands in Plan 04), (2) POST /api/import to trigger one rate-limit-gated round trip to Mono and write rows idempotently, (3) GET /api/transactions to read those rows back on the canonical schema. SC#1, SC#2, SC#3 all become demonstrable. The Phase 1 walking skeleton goes from "the bones exist" (Plans 01 + 02) to "the user-visible capability works" (this plan).

Purpose: Plan 01 gave us schema + structlog + test harness. Plan 02 gave us the rate gate + Mono adapter. This plan binds them — repos, an ImportService that orchestrates discover-accounts-on-first-run + pick-first-card + statement-fetch + ON CONFLICT DO NOTHING insert, and the four FastAPI routes (health, accounts, transactions, import) that expose the capability through HTTP per D-07/D-08/D-09/D-10. Includes the integration test for SC#3 (idempotent repeats) and the integration test for SC#5 (no token / amount in logs across a full import cycle).

Output: An end-to-end testable thin slice. respx-mocked Mono fixtures + testcontainers Postgres prove that POST /api/import + GET /api/transactions round-trip cleanly, that the second POST returns skipped_duplicates>0 with no new rows, and that INFO logs across the full cycle never leak the token, the X-Token header, or any transaction amount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-first-real-transaction/01-CONTEXT.md
@.planning/phases/01-first-real-transaction/01-RESEARCH.md
@.planning/phases/01-first-real-transaction/01-VALIDATION.md
@.planning/phases/01-first-real-transaction/01-SKELETON.md
@.planning/phases/01-first-real-transaction/01-01-PLAN.md
@.planning/phases/01-first-real-transaction/01-02-PLAN.md

<interfaces>
<!-- Imported from prior plans (already exist at execution time) -->

From src/finance_bro/db/models.py (Plan 01):
- Account, Transaction, MonoRateState declarative models

From src/finance_bro/db/engine.py (Plan 01):
- get_session_factory(), set_engine(...)

From src/finance_bro/core/settings.py (Plan 01):
- get_settings() -> Settings(mono_token, database_url, log_level)

From src/finance_bro/core/logging.py (Plan 01):
- configure(level)

From src/finance_bro/importers/base.py (Plan 02):
- CanonicalAccount, CanonicalTransaction, ImporterProtocol

From src/finance_bro/importers/monobank.py (Plan 02):
- MonobankImporter(token, gate)

From src/finance_bro/importers/rate_limit.py (Plan 02):
- RateLimitGate(session_factory), MONO_RATE_LIMIT_SECONDS = 65

<!-- Contracts THIS plan creates (later phases will consume) -->

From src/finance_bro/api/schemas.py (created here):
- TransactionOut(id: int, account_id: int, source_tx_id: str, amount_minor: int, currency: str, time: datetime, raw_payload: dict) — D-10
- AccountOut(id: int, source_kind: str, source_account_id: str, currency: str, masked_pan_tail: str | None) — D-09
- ImportResultOut(polled_account_id: str, statement_count: int, inserted: int, skipped_duplicates: int) — D-08
- HealthOut(status: str, db: str) — D-09

From src/finance_bro/services/import_service.py (created here):
- @dataclass ImportResult(polled_account_id: str, statement_count: int, inserted: int, skipped_duplicates: int)
- class ImportService(session_factory, importer): async run_one_card(now: datetime | None = None) -> ImportResult
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repositories (account_repo, transaction_repo with ON CONFLICT DO NOTHING) + ImportService orchestrator + Pydantic schemas</name>
  <files>
    src/finance_bro/db/account_repo.py,
    src/finance_bro/db/transaction_repo.py,
    src/finance_bro/services/__init__.py,
    src/finance_bro/services/import_service.py,
    src/finance_bro/api/__init__.py,
    src/finance_bro/api/schemas.py
  </files>
  <read_first>
    - .planning/phases/01-first-real-transaction/01-RESEARCH.md (Pattern 3 Synchronous Import Endpoint — INSERT ON CONFLICT DO NOTHING; D-04/D-05/D-06/D-08/D-10 from CONTEXT.md)
    - src/finance_bro/db/models.py (Plan 01)
    - src/finance_bro/importers/base.py (Plan 02 — CanonicalAccount, CanonicalTransaction)
    - src/finance_bro/importers/monobank.py (Plan 02 — MonobankImporter signatures)
  </read_first>
  <behavior>
    - AccountRepo.list_all returns all accounts ordered by id ASC.
    - AccountRepo.get_first_card returns the lowest-id account with source_kind == "mono.card", or None if no card exists.
    - AccountRepo.upsert_many takes list[CanonicalAccount], inserts new rows (matched by uq_accounts_source = source_kind+source_account_id), is idempotent on re-run.
    - TransactionRepo.insert_many takes (account_id: int, list[CanonicalTransaction]) and uses postgresql.insert(...).on_conflict_do_nothing(index_elements=["account_id","source_tx_id"], index_where=text("NOT is_deleted")). Returns (inserted_count: int) where the inserted count is determined by counting rows whose (account_id, source_tx_id) did NOT pre-exist (use RETURNING id and len(returned)).
    - TransactionRepo.list_for_account(account_id: int) returns rows ordered by time DESC with is_deleted=false.
    - ImportService.run_one_card: (a) reads accounts from DB; (b) if empty, instantiates importer, calls discover_accounts (gate already enforced inside importer), upserts all returned accounts, re-reads; (c) picks first mono.card via AccountRepo.get_first_card (raises HTTPException-equivalent ValueError "no card account found" if absent); (d) computes since = now - 31 days, until = now (per Pitfall 5: 31-day window — ample for first slice); (e) calls importer.fetch_statement and collects to list; (f) calls TransactionRepo.insert_many; (g) returns ImportResult(polled_account_id, statement_count, inserted, skipped_duplicates=statement_count-inserted).
    - Pydantic schemas use `int` for amount_minor (not str, not Decimal); currency as `str` constrained to length 3.
  </behavior>
  <action>
    1. Create src/finance_bro/db/account_repo.py:

       from finance_bro.db.models import Account
       from finance_bro.importers.base import CanonicalAccount
       from sqlalchemy import select, text
       from sqlalchemy.dialects.postgresql import insert
       from sqlalchemy.ext.asyncio import AsyncSession

       class AccountRepo:
           def __init__(self, session: AsyncSession):
               self._s = session

           async def list_all(self) -> list[Account]:
               rows = (await self._s.execute(
                   select(Account).order_by(Account.id.asc())
               )).scalars().all()
               return list(rows)

           async def get_first_card(self) -> Account | None:
               return (await self._s.execute(
                   select(Account)
                   .where(Account.source_kind == "mono.card")
                   .order_by(Account.id.asc())
                   .limit(1)
               )).scalar_one_or_none()

           async def upsert_many(self, items: list[CanonicalAccount]) -> int:
               if not items:
                   return 0
               rows = [
                   {
                       "source_kind": a.source_kind,
                       "source_account_id": a.source_account_id,
                       "currency": a.currency,
                       "raw_payload": a.raw,
                   }
                   for a in items
               ]
               stmt = insert(Account).values(rows)
               stmt = stmt.on_conflict_do_nothing(
                   constraint="uq_accounts_source"
               )
               result = await self._s.execute(stmt)
               return result.rowcount or 0

    2. Create src/finance_bro/db/transaction_repo.py — paste pattern from RESEARCH.md Pattern 3 verbatim:

       from finance_bro.db.models import Transaction
       from finance_bro.importers.base import CanonicalTransaction
       from sqlalchemy import select, text
       from sqlalchemy.dialects.postgresql import insert
       from sqlalchemy.ext.asyncio import AsyncSession

       class TransactionRepo:
           def __init__(self, session: AsyncSession):
               self._s = session

           async def insert_many(
               self,
               account_id: int,
               items: list[CanonicalTransaction],
           ) -> int:
               """Returns count of rows actually inserted (skipped duplicates not counted)."""
               if not items:
                   return 0
               rows = [
                   {
                       "account_id": account_id,
                       "source_tx_id": t.source_tx_id,
                       "amount_minor": t.amount_minor,
                       "currency": t.currency,
                       "time": t.occurred_at,
                       "raw_payload": t.raw,
                   }
                   for t in items
               ]
               stmt = insert(Transaction).values(rows)
               stmt = stmt.on_conflict_do_nothing(
                   index_elements=["account_id", "source_tx_id"],
                   index_where=text("NOT is_deleted"),
               ).returning(Transaction.id)
               result = await self._s.execute(stmt)
               returned = result.scalars().all()
               return len(returned)

           async def list_for_account(self, account_id: int) -> list[Transaction]:
               rows = (await self._s.execute(
                   select(Transaction)
                   .where(Transaction.account_id == account_id)
                   .where(Transaction.is_deleted.is_(False))
                   .order_by(Transaction.time.desc())
               )).scalars().all()
               return list(rows)

    3. Create src/finance_bro/services/__init__.py — empty marker.

    4. Create src/finance_bro/services/import_service.py:

       from dataclasses import dataclass
       from datetime import UTC, datetime, timedelta
       from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker
       from finance_bro.db.account_repo import AccountRepo
       from finance_bro.db.transaction_repo import TransactionRepo
       from finance_bro.importers.monobank import MonobankImporter

       MONO_STATEMENT_WINDOW_DAYS = 31  # Pitfall 5

       @dataclass(frozen=True)
       class ImportResult:
           polled_account_id: str
           statement_count: int
           inserted: int
           skipped_duplicates: int

       class NoCardAccountFound(RuntimeError):
           """Raised when client-info returns no account with type=card and we have nothing to poll."""

       class ImportService:
           def __init__(
               self,
               session_factory: async_sessionmaker[AsyncSession],
               importer: MonobankImporter,
           ):
               self._session_factory = session_factory
               self._importer = importer

           async def run_one_card(self, now: datetime | None = None) -> ImportResult:
               now = now or datetime.now(UTC)
               since = now - timedelta(days=MONO_STATEMENT_WINDOW_DAYS)

               # Step 1: ensure accounts table is populated (lazy, D-03 + D-06)
               async with self._session_factory() as session:
                   async with session.begin():
                       accounts = await AccountRepo(session).list_all()
                       have_accounts = bool(accounts)

               if not have_accounts:
                   discovered = await self._importer.discover_accounts()
                   if not discovered:
                       raise NoCardAccountFound("Mono /client-info returned no accounts")
                   async with self._session_factory() as session:
                       async with session.begin():
                           await AccountRepo(session).upsert_many(discovered)

               # Step 2: pick the first card (D-04)
               async with self._session_factory() as session:
                   card = await AccountRepo(session).get_first_card()
               if card is None:
                   raise NoCardAccountFound(
                       "No mono.card account found after discovery. "
                       "Phase 1 polls only cards (D-04)."
                   )

               # Step 3: fetch statement (gate enforced inside importer)
               items = [
                   t async for t in self._importer.fetch_statement(
                       card.source_account_id, since, now,
                   )
               ]

               # Step 4: idempotent insert
               async with self._session_factory() as session:
                   async with session.begin():
                       inserted = await TransactionRepo(session).insert_many(card.id, items)

               return ImportResult(
                   polled_account_id=card.source_account_id,
                   statement_count=len(items),
                   inserted=inserted,
                   skipped_duplicates=len(items) - inserted,
               )

    5. Create src/finance_bro/api/__init__.py — empty marker.

    6. Create src/finance_bro/api/schemas.py:

       from datetime import datetime
       from pydantic import BaseModel, ConfigDict, Field

       class HealthOut(BaseModel):
           status: str
           db: str

       class AccountOut(BaseModel):
           model_config = ConfigDict(from_attributes=True)
           id: int
           source_kind: str
           source_account_id: str
           currency: str = Field(min_length=3, max_length=3)

       class TransactionOut(BaseModel):
           model_config = ConfigDict(from_attributes=True)
           id: int
           account_id: int
           source_tx_id: str
           amount_minor: int
           currency: str = Field(min_length=3, max_length=3)
           time: datetime
           raw_payload: dict

       class ImportResultOut(BaseModel):
           polled_account_id: str
           statement_count: int
           inserted: int
           skipped_duplicates: int
  </action>
  <verify>
    <automated>uv run python -c "from finance_bro.services.import_service import ImportService, ImportResult; from finance_bro.db.account_repo import AccountRepo; from finance_bro.db.transaction_repo import TransactionRepo; from finance_bro.api.schemas import TransactionOut, AccountOut, ImportResultOut, HealthOut; print('imports ok')" &amp;&amp; grep -c 'on_conflict_do_nothing' src/finance_bro/db/transaction_repo.py | grep -q '^1$' &amp;&amp; grep -c 'index_where=text("NOT is_deleted")' src/finance_bro/db/transaction_repo.py | grep -q '^1$' &amp;&amp; grep -c 'amount_minor: int' src/finance_bro/api/schemas.py | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - All imports succeed: `uv run python -c "from finance_bro.services.import_service import ImportService"` exits 0
    - `grep -c 'on_conflict_do_nothing' src/finance_bro/db/transaction_repo.py` returns exactly 1
    - `grep -c 'index_where=text("NOT is_deleted")' src/finance_bro/db/transaction_repo.py` returns exactly 1
    - `grep -c 'amount_minor: int' src/finance_bro/api/schemas.py` returns exactly 1
    - `grep -c 'class ImportService' src/finance_bro/services/import_service.py` returns exactly 1
    - `grep -c 'mono.card' src/finance_bro/services/import_service.py` returns at least 1 (in NoCardAccountFound message)
    - `grep -rEn '\bfloat\(' src/finance_bro/services/ src/finance_bro/db/account_repo.py src/finance_bro/db/transaction_repo.py src/finance_bro/api/schemas.py` returns no matches
    - basedpyright strict passes on the new files
  </acceptance_criteria>
  <done>The orchestration layer exists. Repos use ON CONFLICT DO NOTHING with the partial unique index. ImportService implements lazy discovery (D-03) + first-card pick (D-04) + 31-day window (Pitfall 5) + ON CONFLICT idempotency (ING-04). Pydantic response models declare `amount_minor: int`.</done>
  <threat_refs>T3 (idempotency bypass — ON CONFLICT DO NOTHING is the application path that depends on the DB invariant from Plan 01)</threat_refs>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API routes (health, accounts, transactions, import) + main.py wiring + integration tests for full import cycle, idempotency, and log redaction across the route</name>
  <files>
    src/finance_bro/api/deps.py,
    src/finance_bro/api/routes_health.py,
    src/finance_bro/api/routes_accounts.py,
    src/finance_bro/api/routes_transactions.py,
    src/finance_bro/api/routes_import.py,
    src/finance_bro/main.py,
    tests/test_health.py,
    tests/test_idempotency.py,
    tests/test_import_route.py,
    tests/test_transactions_route.py
  </files>
  <read_first>
    - .planning/phases/01-first-real-transaction/01-CONTEXT.md (D-07, D-08, D-09, D-10 — exact response shapes)
    - .planning/phases/01-first-real-transaction/01-VALIDATION.md (Per-Task Verification Map: ING-03 raw_payload verbatim, ING-03 all accounts persisted, ING-03 transactions route shape, ING-04 idempotency, OPS-04 no token/amount in logs)
    - tests/conftest.py (Plan 01 — confirms `client` fixture wires testcontainers Postgres + ASGI lifespan)
    - src/finance_bro/services/import_service.py (just created Task 1 — ImportService.run_one_card signature)
    - src/finance_bro/api/schemas.py (just created Task 1 — response models)
    - src/finance_bro/main.py (Plan 01 — currently empty app; this task adds include_router calls)
  </read_first>
  <behavior>
    - Test (test_health.py::test_health_db_ok): GET /api/health returns 200 with body {"status":"ok","db":"ok"}.
    - Test (test_health.py::test_health_no_auth): GET /api/health returns 200 even without any auth header.
    - Test (test_import_route.py::test_first_import_discovers_and_inserts): respx mocks /personal/client-info + /personal/statement. POST /api/import returns 200 with body matching ImportResultOut shape; statement_count=2, inserted=2, skipped_duplicates=0.
    - Test (test_import_route.py::test_all_accounts_persisted): after POST /api/import, GET /api/accounts returns 2 entries (one mono.card, one mono.jar) per the client_info_minimal fixture (D-05).
    - Test (test_import_route.py::test_raw_payload_verbatim): after POST /api/import, GET /api/transactions response[i].raw_payload equals the original Mono statementItem dict for both entries (ING-03).
    - Test (test_idempotency.py::test_second_import_is_noop): POST /api/import → POST /api/import again. Second response: inserted=0, skipped_duplicates=2, statement_count=2. GET /api/transactions returns exactly 2 rows (not 4). SC#3 verified.
    - Test (test_transactions_route.py::test_response_shape): after import, each row has int amount_minor, str currency length 3, ISO-8601 time, dict raw_payload. JSON.dumps round-trip preserves int (not float).
    - Test (test_transactions_route.py::test_ordering_time_desc): two transactions with different `time` values come back ordered DESC by `time`.
    - Test (test_import_route.py::test_no_token_in_info_logs_full_cycle): use a structlog test capture (ProcessorFormatter or a list-based wrapper) to capture all INFO log records emitted during a full POST /api/import + GET /api/transactions cycle. Assert: zero records contain the token substring; zero records contain `X-Token`; zero records contain any of the literal `amount` values from the fixture (8500, 5000000, -8500). SC#5 partial (full SC#5 needs `docker logs` against a live container, which Plan 04 owns).
  </behavior>
  <action>
    1. Create src/finance_bro/api/deps.py — FastAPI dependency providers:

       from collections.abc import AsyncIterator
       from fastapi import Depends
       from sqlalchemy.ext.asyncio import AsyncSession
       from finance_bro.core.settings import Settings, get_settings
       from finance_bro.db.engine import get_session_factory
       from finance_bro.importers.monobank import MonobankImporter
       from finance_bro.importers.rate_limit import RateLimitGate
       from finance_bro.services.import_service import ImportService

       async def get_session() -> AsyncIterator[AsyncSession]:
           factory = get_session_factory()
           async with factory() as session:
               yield session

       def get_rate_gate() -> RateLimitGate:
           return RateLimitGate(get_session_factory())

       def get_importer(
           settings: Settings = Depends(get_settings),
           gate: RateLimitGate = Depends(get_rate_gate),
       ) -> MonobankImporter:
           return MonobankImporter(settings.mono_token, gate)

       def get_import_service(
           importer: MonobankImporter = Depends(get_importer),
       ) -> ImportService:
           return ImportService(get_session_factory(), importer)

    2. Create src/finance_bro/api/routes_health.py:

       from fastapi import APIRouter, Depends
       from sqlalchemy import text
       from sqlalchemy.ext.asyncio import AsyncSession
       from finance_bro.api.deps import get_session
       from finance_bro.api.schemas import HealthOut

       router = APIRouter()

       @router.get("/api/health", response_model=HealthOut)
       async def health(session: AsyncSession = Depends(get_session)) -> HealthOut:
           db_status = "ok"
           try:
               await session.execute(text("SELECT 1"))
           except Exception:
               db_status = "error"
           return HealthOut(status="ok", db=db_status)

    3. Create src/finance_bro/api/routes_accounts.py:

       from fastapi import APIRouter, Depends
       from sqlalchemy.ext.asyncio import AsyncSession
       from finance_bro.api.deps import get_session
       from finance_bro.api.schemas import AccountOut
       from finance_bro.db.account_repo import AccountRepo

       router = APIRouter()

       @router.get("/api/accounts", response_model=list[AccountOut])
       async def list_accounts(session: AsyncSession = Depends(get_session)) -> list[AccountOut]:
           rows = await AccountRepo(session).list_all()
           return [AccountOut.model_validate(r) for r in rows]

    4. Create src/finance_bro/api/routes_transactions.py — D-07, D-10:

       from fastapi import APIRouter, Depends, HTTPException
       from sqlalchemy.ext.asyncio import AsyncSession
       from finance_bro.api.deps import get_session
       from finance_bro.api.schemas import TransactionOut
       from finance_bro.db.account_repo import AccountRepo
       from finance_bro.db.transaction_repo import TransactionRepo

       router = APIRouter()

       @router.get("/api/transactions", response_model=list[TransactionOut])
       async def list_transactions(
           session: AsyncSession = Depends(get_session),
       ) -> list[TransactionOut]:
           card = await AccountRepo(session).get_first_card()
           if card is None:
               return []
           rows = await TransactionRepo(session).list_for_account(card.id)
           return [TransactionOut.model_validate(r) for r in rows]

    5. Create src/finance_bro/api/routes_import.py — D-08:

       import structlog
       from fastapi import APIRouter, Depends, HTTPException
       from finance_bro.api.deps import get_import_service
       from finance_bro.api.schemas import ImportResultOut
       from finance_bro.services.import_service import (
           ImportService, NoCardAccountFound,
       )

       router = APIRouter()
       _log = structlog.get_logger()

       @router.post("/api/import", response_model=ImportResultOut)
       async def trigger_import(
           svc: ImportService = Depends(get_import_service),
       ) -> ImportResultOut:
           _log.info("import.start")
           try:
               result = await svc.run_one_card()
           except NoCardAccountFound as e:
               raise HTTPException(status_code=409, detail=str(e)) from e
           _log.info("import.done",
                     polled_account_id=result.polled_account_id,
                     statement_count=result.statement_count,
                     inserted=result.inserted,
                     skipped_duplicates=result.skipped_duplicates)
           return ImportResultOut(
               polled_account_id=result.polled_account_id,
               statement_count=result.statement_count,
               inserted=result.inserted,
               skipped_duplicates=result.skipped_duplicates,
           )

    6. Update src/finance_bro/main.py to include all four routers and wire engine init in lifespan. Replace the Plan-01 placeholder with:

       from contextlib import asynccontextmanager
       from fastapi import FastAPI
       from finance_bro.api import (
           routes_accounts, routes_health, routes_import, routes_transactions,
       )
       from finance_bro.core import logging as logging_cfg
       from finance_bro.core.settings import get_settings
       from finance_bro.db.engine import init_engine

       @asynccontextmanager
       async def lifespan(app: FastAPI):
           settings = get_settings()
           logging_cfg.configure(level=settings.log_level)
           init_engine()
           yield

       app = FastAPI(title="finance-bro", lifespan=lifespan)
       app.include_router(routes_health.router)
       app.include_router(routes_accounts.router)
       app.include_router(routes_transactions.router)
       app.include_router(routes_import.router)

    7. Create tests/test_health.py:

       import pytest

       @pytest.mark.asyncio
       async def test_health_db_ok(client):
           r = await client.get("/api/health")
           assert r.status_code == 200
           assert r.json() == {"status": "ok", "db": "ok"}

       @pytest.mark.asyncio
       async def test_health_no_auth(client):
           r = await client.get("/api/health")
           assert r.status_code == 200
           # no Authorization required

    8. Create tests/test_import_route.py:

       import json
       import logging as stdlog
       from io import StringIO
       from pathlib import Path

       import httpx
       import pytest
       import respx

       FIXTURES = Path(__file__).parent / "fixtures"

       def _client_info():
           return json.loads((FIXTURES / "client_info_minimal.json").read_text())

       def _statement():
           return json.loads((FIXTURES / "statement_two_items.json").read_text())

       @pytest.mark.asyncio
       async def test_first_import_discovers_and_inserts(client):
           # Patch the rate gate to skip sleeping (state still mutates Postgres)
           from unittest.mock import AsyncMock, patch
           with respx.mock(base_url="https://api.monobank.ua") as mock, \
                patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock):
               mock.get("/personal/client-info").mock(
                   return_value=httpx.Response(200, json=_client_info())
               )
               mock.get(url__regex=r"/personal/statement/.*").mock(
                   return_value=httpx.Response(200, json=_statement())
               )
               r = await client.post("/api/import")
           assert r.status_code == 200, r.text
           body = r.json()
           assert body["statement_count"] == 2
           assert body["inserted"] == 2
           assert body["skipped_duplicates"] == 0
           assert body["polled_account_id"] == "card-id-1"

       @pytest.mark.asyncio
       async def test_all_accounts_persisted(client):
           from unittest.mock import AsyncMock, patch
           with respx.mock(base_url="https://api.monobank.ua") as mock, \
                patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock):
               mock.get("/personal/client-info").mock(
                   return_value=httpx.Response(200, json=_client_info())
               )
               mock.get(url__regex=r"/personal/statement/.*").mock(
                   return_value=httpx.Response(200, json=_statement())
               )
               await client.post("/api/import")
           r = await client.get("/api/accounts")
           assert r.status_code == 200
           kinds = sorted(a["source_kind"] for a in r.json())
           assert kinds == ["mono.card", "mono.jar"]

       @pytest.mark.asyncio
       async def test_raw_payload_verbatim(client):
           from unittest.mock import AsyncMock, patch
           statement = _statement()
           with respx.mock(base_url="https://api.monobank.ua") as mock, \
                patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock):
               mock.get("/personal/client-info").mock(
                   return_value=httpx.Response(200, json=_client_info())
               )
               mock.get(url__regex=r"/personal/statement/.*").mock(
                   return_value=httpx.Response(200, json=statement)
               )
               await client.post("/api/import")
           r = await client.get("/api/transactions")
           rows = r.json()
           assert len(rows) == 2
           # statement returned in DESC time order; tx-1 is newer (1746864000 > 1746860400)
           rows_by_id = {row["source_tx_id"]: row for row in rows}
           for original in statement:
               row = rows_by_id[original["id"]]
               assert row["raw_payload"] == original

       @pytest.mark.asyncio
       async def test_no_token_in_info_logs_full_cycle(client, caplog):
           from unittest.mock import AsyncMock, patch
           caplog.set_level(stdlog.INFO)
           # Make the captured token long enough to match the redaction regex (>=30 chars)
           # but the test client uses the conftest-injected token "test-token-aaa..." —
           # we assert that the literal token never reaches caplog.
           import os
           token = os.environ["MONO_TOKEN"]
           assert len(token) >= 30, "Test token must be ≥30 chars to exercise the redaction regex"
           with respx.mock(base_url="https://api.monobank.ua") as mock, \
                patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock):
               mock.get("/personal/client-info").mock(
                   return_value=httpx.Response(200, json=_client_info())
               )
               mock.get(url__regex=r"/personal/statement/.*").mock(
                   return_value=httpx.Response(200, json=_statement())
               )
               await client.post("/api/import")
               await client.get("/api/transactions")
           combined = caplog.text
           assert token not in combined
           assert "X-Token" not in combined
           # Statement amounts must not leak at INFO
           for amount in (-8500, 8500, 5000000):
               assert str(amount) not in combined

    9. Create tests/test_idempotency.py — SC#3:

       import json
       from pathlib import Path
       from unittest.mock import AsyncMock, patch

       import httpx
       import pytest
       import respx

       FIXTURES = Path(__file__).parent / "fixtures"

       @pytest.mark.asyncio
       async def test_second_import_is_noop(client):
           ci = json.loads((FIXTURES / "client_info_minimal.json").read_text())
           stmt = json.loads((FIXTURES / "statement_two_items.json").read_text())
           with respx.mock(base_url="https://api.monobank.ua") as mock, \
                patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock):
               mock.get("/personal/client-info").mock(
                   return_value=httpx.Response(200, json=ci)
               )
               mock.get(url__regex=r"/personal/statement/.*").mock(
                   return_value=httpx.Response(200, json=stmt)
               )
               first = await client.post("/api/import")
               second = await client.post("/api/import")
           assert first.status_code == 200
           assert second.status_code == 200
           f, s = first.json(), second.json()
           assert f["inserted"] == 2 and f["skipped_duplicates"] == 0
           assert s["inserted"] == 0 and s["skipped_duplicates"] == 2
           # And only 2 rows actually exist
           r = await client.get("/api/transactions")
           assert len(r.json()) == 2

    10. Create tests/test_transactions_route.py:

        import json
        from pathlib import Path
        from unittest.mock import AsyncMock, patch

        import httpx
        import pytest
        import respx

        FIXTURES = Path(__file__).parent / "fixtures"

        async def _seed(client):
            with respx.mock(base_url="https://api.monobank.ua") as mock, \
                 patch("finance_bro.importers.rate_limit.asyncio.sleep",
                       new_callable=AsyncMock):
                mock.get("/personal/client-info").mock(
                    return_value=httpx.Response(
                        200, json=json.loads((FIXTURES / "client_info_minimal.json").read_text())
                    )
                )
                mock.get(url__regex=r"/personal/statement/.*").mock(
                    return_value=httpx.Response(
                        200, json=json.loads((FIXTURES / "statement_two_items.json").read_text())
                    )
                )
                await client.post("/api/import")

        @pytest.mark.asyncio
        async def test_response_shape(client):
            await _seed(client)
            r = await client.get("/api/transactions")
            rows = r.json()
            assert len(rows) == 2
            for row in rows:
                assert isinstance(row["amount_minor"], int)
                assert not isinstance(row["amount_minor"], bool)
                assert isinstance(row["currency"], str) and len(row["currency"]) == 3
                assert isinstance(row["raw_payload"], dict)
                # ISO-8601 string is fine; pydantic emits it that way
                assert "T" in row["time"] or "t" in row["time"]

        @pytest.mark.asyncio
        async def test_ordering_time_desc(client):
            await _seed(client)
            r = await client.get("/api/transactions")
            rows = r.json()
            assert rows[0]["source_tx_id"] == "tx-1"  # time=1746864000 (newer)
            assert rows[1]["source_tx_id"] == "tx-2"  # time=1746860400 (older)
  </action>
  <verify>
    <automated>uv run pytest tests/test_health.py tests/test_import_route.py tests/test_idempotency.py tests/test_transactions_route.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_health.py -x` exits 0
    - `uv run pytest tests/test_import_route.py -x` exits 0 (all 4 tests pass)
    - `uv run pytest tests/test_idempotency.py -x` exits 0 (SC#3 demonstrably passes)
    - `uv run pytest tests/test_transactions_route.py -x` exits 0
    - `grep -c 'include_router' src/finance_bro/main.py` returns at least 4
    - `grep -c '/api/health' src/finance_bro/api/routes_health.py` returns exactly 1
    - `grep -c '/api/import' src/finance_bro/api/routes_import.py` returns exactly 1
    - `grep -c '/api/transactions' src/finance_bro/api/routes_transactions.py` returns exactly 1
    - `grep -c '/api/accounts' src/finance_bro/api/routes_accounts.py` returns exactly 1
    - `grep -rn 'token=' src/finance_bro/api/routes_import.py` returns 0 matches (the route never logs the token)
    - `uv run pytest tests/ -v` exits 0 (full suite, all plans 1+2+3 green)
  </acceptance_criteria>
  <done>POST /api/import drives the full Mono cycle synchronously. Second POST returns skipped_duplicates>0 with no new rows. GET /api/transactions returns rows ordered by time DESC with correct shape. GET /api/health returns the compose-friendly payload. INFO logs across the full cycle leak nothing.</done>
  <threat_refs>T1 (full-cycle log redaction), T3 (idempotency end-to-end), T6 (int-only over the JSON boundary)</threat_refs>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN client -> FastAPI route | Untrusted JSON. Pydantic validates request shape (no body in Phase 1 — all reads are GET, the only POST takes no body). |
| API route -> ImportService -> MonobankImporter -> Postgres | All-internal; threats already covered upstream by Plans 01/02. |
| Pydantic response -> JSON network egress | amount_minor MUST serialize as int, never float; raw_payload as dict (JSON object), never as string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 / T1 | Information Disclosure | routes_import structlog event payload | mitigate | `_log.info("import.done", inserted=..., ...)` — no token, no amount_minor in the kwargs. Field names that match `/token|amount/i` are also redacted by the structlog processor (Plan 01 invariant). `tests/test_import_route.py::test_no_token_in_info_logs_full_cycle` asserts zero token / X-Token / amount substrings across a full cycle. |
| T-01-03 / T3 | Tampering | INSERT path in TransactionRepo + uq_transactions_account_source_tx | mitigate | `postgresql.insert(...).on_conflict_do_nothing(index_elements=["account_id","source_tx_id"], index_where=text("NOT is_deleted"))`. Backed by the partial unique index from migration 0001. End-to-end verified by `tests/test_idempotency.py::test_second_import_is_noop` (second POST returns skipped_duplicates=2, transaction count remains 2). |
| T-01-06 / T6 | Tampering | Pydantic response models | mitigate | `TransactionOut.amount_minor: int` (basedpyright strict). JSON serialization preserves int. Verified by `tests/test_transactions_route.py::test_response_shape` (`isinstance(row["amount_minor"], int)`). |
| T-01-XX | Information Disclosure | error responses (NoCardAccountFound) | accept | HTTPException 409 with detail message. Detail does not embed token or sensitive payload — only structural error ("No mono.card account found..."). Acceptable in single-user LAN. |
| T-01-XX | Spoofing | inbound API requests | accept | DEP-02 — no app-level auth. Mitigated at network layer (compose binds 127.0.0.1; LAN/Tailscale gating). Documented in README/CLAUDE.md. |
</threat_model>

<verification>
After both tasks complete:

```bash
# Full suite green (all of Phase 1 plans 01 + 02 + 03)
uv run pytest tests/ -v --tb=short

# Lint + type check
uv run ruff check src/ tests/
uv run ruff format --check src/ tests/
uv run basedpyright src/ tests/

# Behavioral grep gates
grep -c 'on_conflict_do_nothing' src/finance_bro/db/transaction_repo.py | grep -q '^1$'
grep -c 'amount_minor: int' src/finance_bro/api/schemas.py | grep -q '^1$'
grep -rn 'token=' src/finance_bro/api/ | wc -l | grep -q '^[[:space:]]*0$'
grep -rEn '\bfloat\(' src/finance_bro/ | grep -v '^#' | wc -l | grep -q '^[[:space:]]*0$'

# Behavioral smoke (no real Mono call)
uv run python -c "from finance_bro.main import app; print([r.path for r in app.routes if hasattr(r, 'path')])"
```
</verification>

<success_criteria>
- All tests in tests/test_health.py, tests/test_import_route.py, tests/test_idempotency.py, tests/test_transactions_route.py pass
- Combined `uv run pytest tests/` (all 22+ tests across plans 01/02/03) passes
- POST /api/import returns ImportResultOut JSON with the four required keys
- GET /api/transactions returns each row with int `amount_minor`, length-3 `currency`, dict `raw_payload`
- Second POST /api/import is a no-op insert (SC#3)
- INFO log capture across a full import + read cycle contains zero token / X-Token / amount values (SC#5 partial; full Plan 04)
- ruff + basedpyright clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-first-real-transaction/01-03-SUMMARY.md` capturing:
- The four route files + ImportService + repos that wire the slice end-to-end
- The exact JSON shape of POST /api/import response (the prompt for any future tooling that wraps this endpoint)
- The exact JSON shape of GET /api/transactions response (D-10 contract)
- What Plan 04 will verify manually with real Mono + docker compose
</output>
