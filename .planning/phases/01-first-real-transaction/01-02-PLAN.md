---
phase: 01-first-real-transaction
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/finance_bro/importers/__init__.py
  - src/finance_bro/importers/base.py
  - src/finance_bro/importers/currency_map.py
  - src/finance_bro/importers/rate_limit.py
  - src/finance_bro/importers/monobank.py
  - src/finance_bro/db/rate_state_repo.py
  - tests/test_rate_limit_gate.py
  - tests/test_importer_currency_map.py
  - tests/test_importer_statement.py
  - tests/test_importer_no_token_in_url.py
autonomous: true
requirements: [ING-01, ING-02]
requirements_addressed:
  ING-01: "MonobankImporter with discover_accounts and fetch_statement, httpx AsyncClient, X-Token header, numeric to alpha currency at boundary, verbatim raw payload"
  ING-02: "RateLimitGate persisted to Postgres via SELECT FOR UPDATE; single gate per token; restart-safe"
must_haves:
  goals:
    - "SC#4 — two manual imports within 60s do NOT cause Mono 429; gate serializes both callers; persisted to disk so a container restart cannot violate the limit"
  truths:
    - "RateLimitGate.acquire(token) blocks the second caller for at least 60s when called twice in rapid succession"
    - "RateLimitGate state persists across instance recreation: simulating a restart by destroying the gate object and constructing a new one against the same DB still enforces the gap"
    - "Two concurrent RateLimitGate.acquire() calls serialize via SELECT FOR UPDATE; one waits for the other transaction to commit"
    - "MonobankImporter.discover_accounts parses /personal/client-info into CanonicalAccount entries: card -> mono.card, jar -> mono.jar, FOP -> mono.fop"
    - "Mono token rides exclusively in the X-Token header; respx assertion confirms request.url.path never contains the token substring"
    - "Numeric currency codes (980/840/978) are mapped to ISO-4217 alpha (UAH/USD/EUR) at the importer boundary; CanonicalTransaction.currency is always alpha"
    - "amount_minor is always int; no float() anywhere in src/finance_bro/importers/"
    - "Both /personal/client-info and /personal/statement calls go through the SAME RateLimitGate instance (not per-endpoint gates)"
  artifacts:
    - path: "src/finance_bro/importers/base.py"
      provides: "ImporterProtocol + CanonicalAccount + CanonicalTransaction dataclasses; the port for the Mono adapter and any future PrivatBank/Wise importers"
      contains: "class ImporterProtocol(Protocol)"
    - path: "src/finance_bro/importers/rate_limit.py"
      provides: "RateLimitGate(session_factory) with async acquire(token); Postgres FOR UPDATE pattern from RESEARCH.md Pattern 1"
      contains: "FOR UPDATE"
    - path: "src/finance_bro/importers/monobank.py"
      provides: "MonobankImporter; httpx adapter implementing ImporterProtocol"
      contains: "X-Token"
    - path: "src/finance_bro/importers/currency_map.py"
      provides: "numeric_to_alpha(int) -> str; single source of truth for ISO 4217 numeric to alpha"
      contains: "980"
    - path: "src/finance_bro/db/rate_state_repo.py"
      provides: "RateStateRepo wrapping mono_rate_state queries (used by RateLimitGate)"
      contains: "FOR UPDATE"
  key_links:
    - from: "src/finance_bro/importers/monobank.py"
      to: "src/finance_bro/importers/rate_limit.py"
      via: "MonobankImporter.discover_accounts and fetch_statement both await self._gate.acquire(self._token)"
      pattern: "self\\._gate\\.acquire"
    - from: "src/finance_bro/importers/rate_limit.py"
      to: "alembic/versions/0001_walking_skeleton.py (mono_rate_state table)"
      via: "FOR UPDATE row lock on mono_rate_state(token_hash)"
      pattern: "mono_rate_state.*FOR UPDATE"
    - from: "src/finance_bro/importers/monobank.py"
      to: "src/finance_bro/importers/currency_map.py"
      via: "numeric_to_alpha called inside _to_canonical for both account discovery and statement parsing"
      pattern: "numeric_to_alpha\\("
---

<objective>
Land the Mono-side spine: the persistent rate-limit gate and the Mono importer port + adapter, both fully tested against testcontainers Postgres + respx-mocked httpx.

Purpose: ING-02 (rate gate) is the most-quoted Phase 1 pitfall; the in-memory shortcut is the wrong answer because container restarts violate the limit (SC#4 fails). This plan implements the Postgres SELECT FOR UPDATE pattern from RESEARCH.md Pattern 1, owned by MonobankImporter (single gate per token, NOT per endpoint per Pitfall 9). ING-01 is the same Mono adapter that Plan 03 import service will call. The importer port (ImporterProtocol) is the seam Phase 2 scheduler will attach to and where future PrivatBank/Wise importers slot in.

Output: RateLimitGate and MonobankImporter exist as testable units. Six tests prove the rate-gate semantics and importer correctness. No API routes or service wiring yet — Plan 03 owns that.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/01-first-real-transaction/01-CONTEXT.md
@.planning/phases/01-first-real-transaction/01-RESEARCH.md
@.planning/phases/01-first-real-transaction/01-VALIDATION.md
@.planning/phases/01-first-real-transaction/01-SKELETON.md
@.planning/phases/01-first-real-transaction/01-01-PLAN.md

<interfaces>
<!-- Imported from Plan 01 (already exists at execution time) -->

From src/finance_bro/db/models.py (Plan 01):
- class MonoRateState with token_hash (PK Text) and last_acquired_at (TIMESTAMPTZ)

From src/finance_bro/db/engine.py (Plan 01):
- get_session_factory() and set_engine(engine, factory)

From tests/conftest.py (Plan 01):
- pg_url, engine, session_factory, client fixtures shared by all tests

<!-- Contracts THIS plan creates (Plan 03 will consume) -->

From src/finance_bro/importers/base.py (created here):
- @dataclass(frozen=True) CanonicalAccount(source_account_id: str, source_kind: str, currency: str, raw: dict)
- @dataclass(frozen=True) CanonicalTransaction(source_tx_id: str, source_account_id: str, occurred_at: datetime, amount_minor: int, currency: str, raw: dict)
- class ImporterProtocol(Protocol): source_kind: str; async discover_accounts(); fetch_statement(...) -> AsyncIterator

From src/finance_bro/importers/rate_limit.py (created here):
- MONO_RATE_LIMIT_SECONDS = 65
- class RateLimitGate(session_factory) with async acquire(token: str) -> None

From src/finance_bro/importers/monobank.py (created here):
- class MonobankImporter(token: str, gate: RateLimitGate); discover_accounts(); fetch_statement(...); aclose()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Importer port (Protocol + dataclasses), currency_map, RateLimitGate with Postgres FOR UPDATE pattern, and gate tests</name>
  <files>
    src/finance_bro/importers/__init__.py,
    src/finance_bro/importers/base.py,
    src/finance_bro/importers/currency_map.py,
    src/finance_bro/importers/rate_limit.py,
    src/finance_bro/db/rate_state_repo.py,
    tests/test_rate_limit_gate.py,
    tests/test_importer_currency_map.py
  </files>
  <read_first>
    - .planning/phases/01-first-real-transaction/01-RESEARCH.md (Architecture Patterns Pattern 1 Persistent Rate-Limit Gate; Code Examples currency_map.numeric_to_alpha; Pattern 2 Importer Port + Adapter; Pitfall 1 + Pitfall 9 — gate is per-token, not per-endpoint)
    - .planning/phases/01-first-real-transaction/01-CONTEXT.md (Rate-limit bucket persistence discretion)
    - alembic/versions/0001_walking_skeleton.py (created Plan 01; confirms mono_rate_state(token_hash, last_acquired_at) shape)
    - tests/conftest.py (created Plan 01; confirms session_factory fixture signature)
  </read_first>
  <behavior>
    - Test (test_rate_limit_gate.py::test_within_window): create gate, acquire("tok"). Patch asyncio.sleep on the rate_limit module. Acquire again, assert sleep was called once with a value at least 60.
    - Test (test_rate_limit_gate.py::test_persists_across_restart): acquire on gate A, discard A, create gate B against same session_factory; acquire on B; assert sleep was called with at least 60. This proves Pitfall 1 mitigation.
    - Test (test_rate_limit_gate.py::test_concurrent_serialize): asyncio.gather(acquire1, acquire2) from a clean state with sleep patched; assert exactly one of the two calls triggered a sleep of at least 60.
    - Test (test_rate_limit_gate.py::test_different_tokens_independent): acquire("tok-A"); acquire("tok-B") — sleep is NOT called.
    - Test (test_importer_currency_map.py::test_known_codes): numeric_to_alpha(980) == "UAH"; 840 == "USD"; 978 == "EUR".
    - Test (test_importer_currency_map.py::test_unknown_code_raises): numeric_to_alpha(123) raises ValueError whose message contains "123".
  </behavior>
  <action>
    1. Create src/finance_bro/importers/__init__.py — empty marker file.

    2. Create src/finance_bro/importers/base.py with the verbatim contents from RESEARCH.md Pattern 2:

       from collections.abc import AsyncIterator
       from dataclasses import dataclass
       from datetime import datetime
       from typing import Protocol

       @dataclass(frozen=True)
       class CanonicalAccount:
           source_account_id: str
           source_kind: str
           currency: str
           raw: dict

       @dataclass(frozen=True)
       class CanonicalTransaction:
           source_tx_id: str
           source_account_id: str
           occurred_at: datetime
           amount_minor: int
           currency: str
           raw: dict

       class ImporterProtocol(Protocol):
           source_kind: str

           async def discover_accounts(self) -> list[CanonicalAccount]: ...

           def fetch_statement(
               self,
               source_account_id: str,
               since: datetime,
               until: datetime,
           ) -> AsyncIterator[CanonicalTransaction]: ...

    3. Create src/finance_bro/importers/currency_map.py with the verbatim contents from RESEARCH.md Code Examples:

       _NUM_TO_ALPHA: dict[int, str] = {
           980: "UAH",
           840: "USD",
           978: "EUR",
       }

       def numeric_to_alpha(code: int) -> str:
           try:
               return _NUM_TO_ALPHA[code]
           except KeyError as e:
               raise ValueError(f"Unknown ISO 4217 numeric code: {code}") from e

    4. Create src/finance_bro/db/rate_state_repo.py — async repository over mono_rate_state. Per CONTEXT.md "Rate-limit bucket persistence":

       from datetime import datetime
       from sqlalchemy import text
       from sqlalchemy.ext.asyncio import AsyncSession

       class RateStateRepo:
           """Single owner of writes to mono_rate_state. RateLimitGate uses this."""

           def __init__(self, session: AsyncSession):
               self._s = session

           async def select_for_update(self, token_hash: str) -> datetime | None:
               row = (await self._s.execute(
                   text(
                       "SELECT last_acquired_at FROM mono_rate_state "
                       "WHERE token_hash = :h FOR UPDATE"
                   ),
                   {"h": token_hash},
               )).first()
               return row[0] if row else None

           async def upsert(self, token_hash: str, ts: datetime) -> None:
               await self._s.execute(
                   text(
                       "INSERT INTO mono_rate_state (token_hash, last_acquired_at) "
                       "VALUES (:h, :ts) "
                       "ON CONFLICT (token_hash) DO UPDATE "
                       "SET last_acquired_at = EXCLUDED.last_acquired_at"
                   ),
                   {"h": token_hash, "ts": ts},
               )

    5. Create src/finance_bro/importers/rate_limit.py — paste pattern from RESEARCH.md Pattern 1, refactored to use RateStateRepo so SQL stays in db/:

       import asyncio
       import hashlib
       from datetime import UTC, datetime, timedelta
       from sqlalchemy.ext.asyncio import AsyncSession, async_sessionmaker
       from finance_bro.db.rate_state_repo import RateStateRepo

       MONO_RATE_LIMIT_SECONDS = 65  # 60s API limit + 5s slack for clock drift

       class RateLimitGate:
           """Postgres-backed token-bucket gate. Single instance per token. Persists
           last_acquired_at in mono_rate_state so a container restart cannot violate
           Mono 1 req / 60s rule (Pitfall 1). Uses SELECT FOR UPDATE so concurrent
           acquirers serialize (Pattern 1)."""

           def __init__(self, session_factory: async_sessionmaker[AsyncSession]):
               self._session_factory = session_factory

           async def acquire(self, token: str) -> None:
               token_hash = hashlib.sha256(token.encode()).hexdigest()
               now = datetime.now(UTC)
               wait_until: datetime | None = None

               async with self._session_factory() as session:
                   async with session.begin():
                       repo = RateStateRepo(session)
                       last = await repo.select_for_update(token_hash)
                       if last is not None:
                           if last.tzinfo is None:
                               last = last.replace(tzinfo=UTC)
                           next_allowed = last + timedelta(seconds=MONO_RATE_LIMIT_SECONDS)
                           if next_allowed > now:
                               wait_until = next_allowed
                       claim_ts = wait_until or now
                       await repo.upsert(token_hash, claim_ts)

               if wait_until is not None:
                   remaining = (wait_until - datetime.now(UTC)).total_seconds()
                   if remaining > 0:
                       await asyncio.sleep(remaining)

    6. Create tests/test_rate_limit_gate.py with all four tests above. Use unittest.mock.AsyncMock + patch on "finance_bro.importers.rate_limit.asyncio.sleep". Reference test contents in the behavior block; below is the full file body to write:

       import asyncio
       from unittest.mock import AsyncMock, patch
       import pytest

       @pytest.mark.asyncio
       async def test_within_window(session_factory):
           from finance_bro.importers.rate_limit import RateLimitGate
           gate = RateLimitGate(session_factory)
           with patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock) as mock_sleep:
               await gate.acquire("tok-aaa")
               await gate.acquire("tok-aaa")
           assert mock_sleep.await_count == 1
           assert mock_sleep.await_args.args[0] >= 60

       @pytest.mark.asyncio
       async def test_persists_across_restart(session_factory):
           from finance_bro.importers.rate_limit import RateLimitGate
           gate_a = RateLimitGate(session_factory)
           with patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock):
               await gate_a.acquire("tok-restart")
           del gate_a
           gate_b = RateLimitGate(session_factory)
           with patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock) as mock_sleep:
               await gate_b.acquire("tok-restart")
           assert mock_sleep.await_count == 1
           assert mock_sleep.await_args.args[0] >= 60

       @pytest.mark.asyncio
       async def test_concurrent_serialize(session_factory):
           from finance_bro.importers.rate_limit import RateLimitGate
           gate = RateLimitGate(session_factory)
           with patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock) as mock_sleep:
               await asyncio.gather(
                   gate.acquire("tok-concurrent"),
                   gate.acquire("tok-concurrent"),
               )
           assert mock_sleep.await_count == 1, (
               f"Expected exactly one sleep, got {mock_sleep.await_count}"
           )
           assert mock_sleep.await_args.args[0] >= 60

       @pytest.mark.asyncio
       async def test_different_tokens_independent(session_factory):
           from finance_bro.importers.rate_limit import RateLimitGate
           gate = RateLimitGate(session_factory)
           with patch("finance_bro.importers.rate_limit.asyncio.sleep",
                      new_callable=AsyncMock) as mock_sleep:
               await gate.acquire("token-A-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa")
               await gate.acquire("token-B-bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb")
           assert mock_sleep.await_count == 0

    7. Create tests/test_importer_currency_map.py:

       import pytest

       def test_known_codes():
           from finance_bro.importers.currency_map import numeric_to_alpha
           assert numeric_to_alpha(980) == "UAH"
           assert numeric_to_alpha(840) == "USD"
           assert numeric_to_alpha(978) == "EUR"

       def test_unknown_code_raises():
           from finance_bro.importers.currency_map import numeric_to_alpha
           with pytest.raises(ValueError) as exc:
               numeric_to_alpha(123)
           assert "123" in str(exc.value)
  </action>
  <verify>
    <automated>uv run pytest tests/test_rate_limit_gate.py tests/test_importer_currency_map.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_rate_limit_gate.py -x` exits 0 (all 4 tests pass)
    - `uv run pytest tests/test_importer_currency_map.py -x` exits 0 (both tests pass)
    - `grep -c 'FOR UPDATE' src/finance_bro/db/rate_state_repo.py` returns at least 1
    - `grep -c 'MONO_RATE_LIMIT_SECONDS = 65' src/finance_bro/importers/rate_limit.py` returns exactly 1
    - `grep -c 'class RateLimitGate' src/finance_bro/importers/rate_limit.py` returns exactly 1
    - `grep -c 'class ImporterProtocol(Protocol)' src/finance_bro/importers/base.py` returns exactly 1
    - `grep -c '980: "UAH"' src/finance_bro/importers/currency_map.py` returns exactly 1
    - `grep -rEn '\bfloat\(' src/finance_bro/importers/` returns no matches (exit 1)
  </acceptance_criteria>
  <done>Importer port + currency map + RateLimitGate land with all four gate tests green. The Postgres FOR UPDATE pattern is locked; container-restart safety, within-window blocking, and concurrent serialization are proven.</done>
  <threat_refs>T4 (rate-limit bypass / Mono 429), T5 (container restart violates rate limit)</threat_refs>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MonobankImporter httpx adapter (X-Token header, numeric to alpha, verbatim raw payload) and importer tests with respx</name>
  <files>
    src/finance_bro/importers/monobank.py,
    tests/test_importer_statement.py,
    tests/test_importer_no_token_in_url.py
  </files>
  <read_first>
    - .planning/phases/01-first-real-transaction/01-RESEARCH.md (Code Examples — MonobankImporter.discover_accounts verbatim; Pitfall 7 — token never in URL; Pitfall 3 — currency mapping at boundary)
    - tests/fixtures/client_info_minimal.json (Plan 01 created — confirm shape)
    - tests/fixtures/statement_two_items.json (Plan 01 created — confirm shape)
    - src/finance_bro/importers/base.py (just created Task 1 — confirms CanonicalAccount/CanonicalTransaction shape)
    - src/finance_bro/importers/rate_limit.py (just created Task 1 — confirms RateLimitGate.acquire signature)
  </read_first>
  <behavior>
    - Test (test_importer_statement.py::test_discover_accounts_maps_kinds): respx mocks GET /personal/client-info to return fixtures/client_info_minimal.json. discover_accounts() returns one CanonicalAccount(source_kind="mono.card", currency="UAH") and one CanonicalAccount(source_kind="mono.jar", currency="UAH"). The card and the jar are both ISO-4217 alpha "UAH" (numeric 980 mapped at boundary).
    - Test (test_importer_statement.py::test_discover_accounts_fop_kind): respx mocks client-info with one account whose type == "fop"; that becomes source_kind="mono.fop".
    - Test (test_importer_statement.py::test_fetch_statement_yields_canonical): respx mocks GET /personal/statement/{account}/{from}/{to} returning fixtures/statement_two_items.json. fetch_statement(...) yields two CanonicalTransaction with amount_minor as int (no float), currency "UAH", raw == verbatim Mono item dict.
    - Test (test_importer_statement.py::test_raw_payload_verbatim): for each yielded CanonicalTransaction, ct.raw equals the literal dict from the fixture (json.loads round-trip equal).
    - Test (test_importer_statement.py::test_amount_minor_is_int_not_float): assert isinstance(ct.amount_minor, int) and not isinstance(ct.amount_minor, float) — guards against int subclass vs float drift.
    - Test (test_importer_statement.py::test_calls_through_gate): both discover_accounts and fetch_statement must call gate.acquire(token) before issuing the HTTP request. Use a stub gate that records calls; assert order: gate.acquire called BEFORE respx records the HTTP request.
    - Test (test_importer_no_token_in_url.py::test_token_only_in_header_for_client_info): after a discover_accounts call, respx_mock.calls[0].request.url path/query MUST NOT contain the token substring. Header X-Token MUST equal the token.
    - Test (test_importer_no_token_in_url.py::test_token_only_in_header_for_statement): same assertion for fetch_statement.
  </behavior>
  <action>
    Create src/finance_bro/importers/monobank.py — paste from RESEARCH.md Code Examples MonobankImporter.discover_accounts verbatim, with FOP discrimination per the discretion in CONTEXT.md (account.type == "fop" -> mono.fop; otherwise mono.card):

    import httpx
    from collections.abc import AsyncIterator
    from datetime import UTC, datetime
    from .base import CanonicalAccount, CanonicalTransaction
    from .currency_map import numeric_to_alpha
    from .rate_limit import RateLimitGate

    MONO_BASE = "https://api.monobank.ua"

    class MonobankImporter:
        source_kind = "monobank"

        def __init__(self, token: str, gate: RateLimitGate):
            self._token = token
            self._gate = gate
            self._client = httpx.AsyncClient(
                base_url=MONO_BASE,
                timeout=httpx.Timeout(30.0, connect=10.0),
                headers={"X-Token": token},
            )

        async def aclose(self) -> None:
            await self._client.aclose()

        async def discover_accounts(self) -> list[CanonicalAccount]:
            await self._gate.acquire(self._token)
            resp = await self._client.get("/personal/client-info")
            resp.raise_for_status()
            data = resp.json()
            out: list[CanonicalAccount] = []
            for acc in data.get("accounts", []):
                kind = "mono.fop" if acc.get("type") == "fop" else "mono.card"
                out.append(CanonicalAccount(
                    source_account_id=acc["id"],
                    source_kind=kind,
                    currency=numeric_to_alpha(acc["currencyCode"]),
                    raw=acc,
                ))
            for jar in data.get("jars", []):
                out.append(CanonicalAccount(
                    source_account_id=jar["id"],
                    source_kind="mono.jar",
                    currency=numeric_to_alpha(jar["currencyCode"]),
                    raw=jar,
                ))
            return out

        async def fetch_statement(
            self,
            source_account_id: str,
            since: datetime,
            until: datetime,
        ) -> AsyncIterator[CanonicalTransaction]:
            await self._gate.acquire(self._token)
            from_ts = int(since.timestamp())
            to_ts = int(until.timestamp())
            resp = await self._client.get(
                f"/personal/statement/{source_account_id}/{from_ts}/{to_ts}"
            )
            resp.raise_for_status()
            for item in resp.json():
                yield CanonicalTransaction(
                    source_tx_id=item["id"],
                    source_account_id=source_account_id,
                    occurred_at=datetime.fromtimestamp(item["time"], tz=UTC),
                    amount_minor=int(item["amount"]),
                    currency=numeric_to_alpha(item["currencyCode"]),
                    raw=item,
                )

    Note: per Pitfall 7, the token rides in headers={"X-Token": token} only; route URLs are formatted from source_account_id, from_ts, to_ts. No string concatenation puts the token in a URL.

    Create tests/test_importer_statement.py:

    import json
    from datetime import UTC, datetime
    from pathlib import Path
    from unittest.mock import AsyncMock

    import httpx
    import pytest
    import respx

    FIXTURES = Path(__file__).parent / "fixtures"

    @pytest.fixture
    def stub_gate():
        g = AsyncMock()
        g.acquire = AsyncMock(return_value=None)
        return g

    @pytest.mark.asyncio
    async def test_discover_accounts_maps_kinds(stub_gate):
        from finance_bro.importers.monobank import MonobankImporter
        payload = json.loads((FIXTURES / "client_info_minimal.json").read_text())
        with respx.mock(base_url="https://api.monobank.ua") as mock:
            mock.get("/personal/client-info").mock(
                return_value=httpx.Response(200, json=payload)
            )
            imp = MonobankImporter("test-token-aaaaaaaaaaaaaaaaaaaaaaaaaaaa", stub_gate)
            accounts = await imp.discover_accounts()
            await imp.aclose()
        kinds = sorted(a.source_kind for a in accounts)
        assert kinds == ["mono.card", "mono.jar"]
        for a in accounts:
            assert a.currency == "UAH"
        stub_gate.acquire.assert_awaited_once()

    @pytest.mark.asyncio
    async def test_discover_accounts_fop_kind(stub_gate):
        from finance_bro.importers.monobank import MonobankImporter
        payload = {
            "clientId": "c", "name": "n", "webHookUrl": "", "permissions": "rsf",
            "accounts": [{"id": "fop-1", "type": "fop", "currencyCode": 980,
                          "balance": 0, "creditLimit": 0, "sendId": "s",
                          "cashbackType": "UAH", "maskedPan": [], "iban": ""}],
            "jars": [],
        }
        with respx.mock(base_url="https://api.monobank.ua") as mock:
            mock.get("/personal/client-info").mock(
                return_value=httpx.Response(200, json=payload)
            )
            imp = MonobankImporter("test-token-aaaaaaaaaaaaaaaaaaaaaaaaaaaa", stub_gate)
            accounts = await imp.discover_accounts()
            await imp.aclose()
        assert len(accounts) == 1
        assert accounts[0].source_kind == "mono.fop"

    @pytest.mark.asyncio
    async def test_fetch_statement_yields_canonical(stub_gate):
        from finance_bro.importers.monobank import MonobankImporter
        payload = json.loads((FIXTURES / "statement_two_items.json").read_text())
        with respx.mock(base_url="https://api.monobank.ua") as mock:
            mock.get(url__regex=r"/personal/statement/acc-1/.*").mock(
                return_value=httpx.Response(200, json=payload)
            )
            imp = MonobankImporter("test-token-aaaaaaaaaaaaaaaaaaaaaaaaaaaa", stub_gate)
            since = datetime(2026, 5, 9, tzinfo=UTC)
            until = datetime(2026, 5, 10, tzinfo=UTC)
            txs = [t async for t in imp.fetch_statement("acc-1", since, until)]
            await imp.aclose()
        assert len(txs) == 2
        assert txs[0].source_tx_id == "tx-1"
        assert txs[0].amount_minor == -8500
        assert txs[0].currency == "UAH"
        assert txs[1].source_tx_id == "tx-2"
        assert txs[1].amount_minor == 5000000

    @pytest.mark.asyncio
    async def test_raw_payload_verbatim(stub_gate):
        from finance_bro.importers.monobank import MonobankImporter
        payload = json.loads((FIXTURES / "statement_two_items.json").read_text())
        with respx.mock(base_url="https://api.monobank.ua") as mock:
            mock.get(url__regex=r"/personal/statement/.*").mock(
                return_value=httpx.Response(200, json=payload)
            )
            imp = MonobankImporter("test-token-aaaaaaaaaaaaaaaaaaaaaaaaaaaa", stub_gate)
            txs = [t async for t in imp.fetch_statement(
                "acc-1", datetime(2026, 5, 9, tzinfo=UTC), datetime(2026, 5, 10, tzinfo=UTC),
            )]
            await imp.aclose()
        for ct, original in zip(txs, payload, strict=True):
            assert ct.raw == original

    @pytest.mark.asyncio
    async def test_amount_minor_is_int_not_float(stub_gate):
        from finance_bro.importers.monobank import MonobankImporter
        payload = json.loads((FIXTURES / "statement_two_items.json").read_text())
        with respx.mock(base_url="https://api.monobank.ua") as mock:
            mock.get(url__regex=r"/personal/statement/.*").mock(
                return_value=httpx.Response(200, json=payload)
            )
            imp = MonobankImporter("test-token-aaaaaaaaaaaaaaaaaaaaaaaaaaaa", stub_gate)
            txs = [t async for t in imp.fetch_statement(
                "a", datetime(2026, 5, 9, tzinfo=UTC), datetime(2026, 5, 10, tzinfo=UTC),
            )]
            await imp.aclose()
        for ct in txs:
            assert isinstance(ct.amount_minor, int)
            assert not isinstance(ct.amount_minor, bool)
            assert type(ct.amount_minor) is int

    @pytest.mark.asyncio
    async def test_calls_through_gate(stub_gate):
        from finance_bro.importers.monobank import MonobankImporter
        client_info_payload = json.loads((FIXTURES / "client_info_minimal.json").read_text())
        with respx.mock(base_url="https://api.monobank.ua") as mock:
            ci = mock.get("/personal/client-info").mock(
                return_value=httpx.Response(200, json=client_info_payload)
            )
            imp = MonobankImporter("test-token-aaaaaaaaaaaaaaaaaaaaaaaaaaaa", stub_gate)
            await imp.discover_accounts()
            await imp.aclose()
        # Gate was called before HTTP fired
        stub_gate.acquire.assert_awaited_once()
        assert ci.called

    Create tests/test_importer_no_token_in_url.py:

    import json
    from datetime import UTC, datetime
    from pathlib import Path
    from unittest.mock import AsyncMock

    import httpx
    import pytest
    import respx

    FIXTURES = Path(__file__).parent / "fixtures"
    TOKEN = "test-token-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"

    @pytest.fixture
    def stub_gate():
        g = AsyncMock()
        g.acquire = AsyncMock(return_value=None)
        return g

    @pytest.mark.asyncio
    async def test_token_only_in_header_for_client_info(stub_gate):
        from finance_bro.importers.monobank import MonobankImporter
        payload = json.loads((FIXTURES / "client_info_minimal.json").read_text())
        with respx.mock(base_url="https://api.monobank.ua") as mock:
            route = mock.get("/personal/client-info").mock(
                return_value=httpx.Response(200, json=payload)
            )
            imp = MonobankImporter(TOKEN, stub_gate)
            await imp.discover_accounts()
            await imp.aclose()
        req = route.calls[0].request
        url_str = str(req.url)
        assert TOKEN not in url_str, f"Token must never appear in URL; saw {url_str}"
        assert req.headers.get("X-Token") == TOKEN

    @pytest.mark.asyncio
    async def test_token_only_in_header_for_statement(stub_gate):
        from finance_bro.importers.monobank import MonobankImporter
        payload = json.loads((FIXTURES / "statement_two_items.json").read_text())
        with respx.mock(base_url="https://api.monobank.ua") as mock:
            route = mock.get(url__regex=r"/personal/statement/.*").mock(
                return_value=httpx.Response(200, json=payload)
            )
            imp = MonobankImporter(TOKEN, stub_gate)
            txs = [t async for t in imp.fetch_statement(
                "acc-x", datetime(2026, 5, 9, tzinfo=UTC), datetime(2026, 5, 10, tzinfo=UTC),
            )]
            await imp.aclose()
        assert len(txs) == 2
        req = route.calls[0].request
        url_str = str(req.url)
        assert TOKEN not in url_str
        assert "acc-x" in url_str  # the account ID DOES appear in URL — that's fine
        assert req.headers.get("X-Token") == TOKEN
  </action>
  <verify>
    <automated>uv run pytest tests/test_importer_statement.py tests/test_importer_no_token_in_url.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_importer_statement.py -x` exits 0 (all 6 tests pass)
    - `uv run pytest tests/test_importer_no_token_in_url.py -x` exits 0 (both tests pass)
    - `grep -c 'X-Token' src/finance_bro/importers/monobank.py` returns exactly 1
    - `grep -cE 'f["\x27].*\{token\}.*["\x27]' src/finance_bro/importers/monobank.py` returns 0 (no f-strings with token in URL)
    - `grep -c 'await self._gate.acquire' src/finance_bro/importers/monobank.py` returns at least 2 (one for client-info, one for statement)
    - `grep -c 'numeric_to_alpha(' src/finance_bro/importers/monobank.py` returns at least 3 (cards, jars, statement)
    - `grep -rEn '\bfloat\(' src/finance_bro/importers/` returns no matches (exit 1)
  </acceptance_criteria>
  <done>MonobankImporter is the only place numeric currency codes get mapped to alpha. Token rides only in X-Token header (verified by respx assertions). Both endpoints route through the SAME RateLimitGate.acquire(). amount_minor is always int. raw payload is verbatim.</done>
  <threat_refs>T1 (token in URL is a leakage vector), T6 (money precision; int not float)</threat_refs>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App process -> api.monobank.ua | HTTPS egress. Token in X-Token header, never in URL. |
| App process -> Postgres (mono_rate_state) | Local Docker network; FOR UPDATE serializes concurrent writers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 / T1 | Information Disclosure | MonobankImporter HTTP request construction | mitigate | httpx AsyncClient receives `headers={"X-Token": token}` once at instantiation. Route URLs are formatted from source_account_id + from_ts + to_ts only. Verified by `tests/test_importer_no_token_in_url.py` (asserts `TOKEN not in str(request.url)` for both client-info and statement). Pitfall 7 closed. |
| T-01-04 / T4 | Denial of Service / Tampering | RateLimitGate / mono_rate_state | mitigate | Postgres `SELECT ... FOR UPDATE` row lock (RESEARCH.md Pattern 1). Optimistic claim of the *next* slot inside the transaction so concurrent acquirers serialize. Verified by `tests/test_rate_limit_gate.py::test_concurrent_serialize` and `::test_within_window`. Single gate per token; both client-info and statement use the same gate (Pitfall 9 closed). |
| T-01-05 / T5 | Denial of Service | RateLimitGate persistence | mitigate | State lives in `mono_rate_state` Postgres row, NEVER in process memory. `tests/test_rate_limit_gate.py::test_persists_across_restart` simulates a process boundary (delete gate A, instantiate gate B against same DB) and asserts the gap is still enforced. Pitfall 1 closed. |
| T-01-06 / T6 | Tampering | money pipeline (importer side) | mitigate | `int(item["amount"])` in MonobankImporter; CanonicalTransaction.amount_minor typed `int`. `tests/test_importer_statement.py::test_amount_minor_is_int_not_float` asserts type. Static grep gate: `grep -rEn '\bfloat\(' src/finance_bro/importers/` returns zero matches. |
| T-01-XX | Information Disclosure | structlog at DEBUG | accept | DEBUG bypasses redaction by design (RESEARCH.md Pattern 4). Documented in README/CLAUDE.md as local-dev only; production runs at INFO. |
</threat_model>

<verification>
After both tasks complete:

```bash
# Full suite for this plan
uv run pytest tests/test_rate_limit_gate.py tests/test_importer_currency_map.py tests/test_importer_statement.py tests/test_importer_no_token_in_url.py -v --tb=short

# Lint + type check
uv run ruff check src/finance_bro/importers/ src/finance_bro/db/rate_state_repo.py tests/test_rate_limit_gate.py tests/test_importer_currency_map.py tests/test_importer_statement.py tests/test_importer_no_token_in_url.py
uv run basedpyright src/finance_bro/importers/

# Invariant grep gates
grep -rEn '\bfloat\(' src/finance_bro/importers/ | grep -v '^#' | wc -l | grep -q '^[[:space:]]*0$'
grep -c 'FOR UPDATE' src/finance_bro/db/rate_state_repo.py | grep -q '^[1-9]'
grep -c 'X-Token' src/finance_bro/importers/monobank.py | grep -q '^1$'
```
</verification>

<success_criteria>
- All 12 tests in this plan green: 4 in test_rate_limit_gate.py, 2 in test_importer_currency_map.py, 6 in test_importer_statement.py, 2 in test_importer_no_token_in_url.py
- RateLimitGate proven to (a) block within window, (b) persist across instance recreation, (c) serialize concurrent acquirers, (d) treat different tokens independently
- MonobankImporter proven to (a) map account kinds correctly (mono.card / mono.jar / mono.fop), (b) yield CanonicalTransaction with int amount_minor and alpha currency and verbatim raw, (c) call gate.acquire BEFORE issuing HTTP, (d) keep token in X-Token header only
- ruff + basedpyright clean
- Zero `float(` usage in src/finance_bro/importers/
</success_criteria>

<output>
After completion, create `.planning/phases/01-first-real-transaction/01-02-SUMMARY.md` capturing:
- The two new modules: rate_limit.py and monobank.py
- The exact contracts Plan 03 will consume: `RateLimitGate(session_factory)`, `MonobankImporter(token, gate)` with `.discover_accounts()` -> `list[CanonicalAccount]` and `.fetch_statement(...)` -> `AsyncIterator[CanonicalTransaction]`
- The key insight from Pattern 1: the timestamp written to mono_rate_state is the time at which the *next* allowed acquire begins, not "now" — this is what makes concurrent acquirers serialize correctly
- Open questions to log empirically: type enum values for FOP accounts (Assumption A5)
</output>
