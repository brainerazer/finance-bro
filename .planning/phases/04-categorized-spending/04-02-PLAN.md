---
phase: 04-categorized-spending
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/finance_bro/db/category_repo.py
  - src/finance_bro/db/rule_repo.py
  - src/finance_bro/db/transaction_repo.py
  - src/finance_bro/api/schemas.py
  - src/finance_bro/api/routes_categories.py
  - src/finance_bro/api/routes_rules.py
  - src/finance_bro/api/deps.py
  - src/finance_bro/main.py
  - tests/test_categories_crud.py
  - tests/test_rules_crud.py
  - tests/test_category_delete_guard.py
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
must_haves:
  truths:
    - "Caller can create, list, rename, and recolor categories via the API"
    - "Caller can create a rule with a validated predicate, list rules priority-ordered, and reorder priorities"
    - "Deleting a category referenced by any rule or transaction returns 409 with reference counts"
    - "The transactions read path carries each row's category id/name/color"
  artifacts:
    - path: "src/finance_bro/db/category_repo.py"
      provides: "Category CRUD + reference_counts pre-check (D-15)"
      exports: ["reference_counts"]
    - path: "src/finance_bro/db/rule_repo.py"
      provides: "Rule CRUD, list_active_ordered (priority ASC, id ASC), priority reorder"
      exports: ["list_active_ordered"]
    - path: "src/finance_bro/api/routes_categories.py"
      provides: "POST/PATCH/DELETE/list /api/categories with 409 delete guard"
    - path: "src/finance_bro/api/routes_rules.py"
      provides: "POST/PATCH/DELETE/list /api/rules + priority reorder"
  key_links:
    - from: "src/finance_bro/api/routes_categories.py"
      to: "src/finance_bro/db/category_repo.py"
      via: "reference_counts pre-check before delete → 409"
      pattern: "reference_counts"
    - from: "src/finance_bro/api/routes_rules.py"
      to: "src/finance_bro/categorizer/predicate.py"
      via: "RulePredicate validation at request parse"
      pattern: "RulePredicate"
    - from: "src/finance_bro/main.py"
      to: "src/finance_bro/api/routes_categories.py"
      via: "app.include_router"
      pattern: "routes_categories"
---

<objective>
Deliver the Category + Rule CRUD slice end-to-end: repos, request/response DTOs (including the predicate AST at the API boundary), the `routes_categories.py` and `routes_rules.py` routers (mounted in `main.py`), the D-15 delete guard (409 with reference counts), priority reorder, and extending the transactions read path to carry category data. A caller can fully manage the taxonomy and rule set over HTTP after this plan.

Purpose: CAT-02 (user-controlled priority list) and CAT-03 (user-editable categories) become real API surfaces; rules are authored with predicates validated at the boundary (V5 input validation) so a malformed predicate is rejected before it can reach the interpreter.

Output: Two new repos, two new routers wired into `main.py`, extended `schemas.py` + `transaction_repo` read, and three green integration tests (`test_categories_crud.py`, `test_rules_crud.py`, `test_category_delete_guard.py`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-categorized-spending/04-CONTEXT.md
@.planning/phases/04-categorized-spending/04-RESEARCH.md
@.planning/phases/04-categorized-spending/04-PATTERNS.md
@.planning/phases/04-categorized-spending/04-01-SUMMARY.md

<interfaces>
<!-- Predicate AST from Plan 01 (src/finance_bro/categorizer/predicate.py): -->
<!-- RulePredicate(all: list[Condition]) where Condition is the discriminated union. -->
<!-- Reuse RulePredicate directly as the request DTO field for a rule's predicate. -->

<!-- Repo constructor idiom (tracked_fx_currency_repo.py:22-24): def __init__(self, session: AsyncSession): self._s = session -->
<!-- Ordered list read (tracked_fx_currency_repo.py:26-30): select(Model).order_by(...) -->
<!-- text() bound-param count (fx_rate_repo count_in_window): text("SELECT count(*) ... WHERE col=:p", {"p": v}) -->

<!-- Router idiom (routes_transactions.py:16-37 / routes_backfill.py:26-54): -->
<!-- router = APIRouter(); @router.get("/api/...", response_model=...); session: Annotated[AsyncSession, Depends(get_session)] -->
<!-- HTTPException(status_code=409, detail={...}) for the D-15 guard -->

<!-- main.py wiring: import tuple at lines 39-45; app.include_router(...) block at lines 138-143 -->

<!-- transaction_repo ROLLUP_SQL (lines 32-46): LEFT JOIN LATERAL fx; extend with LEFT JOIN categories c ON c.id = t.category_id -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Category + Rule repos + DTOs + transactions read extension</name>
  <files>src/finance_bro/db/category_repo.py, src/finance_bro/db/rule_repo.py, src/finance_bro/db/transaction_repo.py, src/finance_bro/api/schemas.py</files>
  <read_first>
    - src/finance_bro/db/tracked_fx_currency_repo.py (lines 15-57 — EXACT repo template: constructor, ordered list, single-row get, text() targeted update)
    - src/finance_bro/db/fx_rate_repo.py (count_in_window — the parameterized text() count idiom for reference_counts)
    - src/finance_bro/db/transaction_repo.py (lines 32-46 ROLLUP_SQL to extend; lines 49-59 .get() safety)
    - src/finance_bro/api/schemas.py (lines 40-61 TransactionOut from_attributes DTO; lines 124-126 BackfillEnqueueIn bounded request DTO)
    - src/finance_bro/categorizer/predicate.py (RulePredicate — reuse as the rule request DTO predicate field)
    - .planning/phases/04-categorized-spending/04-CONTEXT.md (D-02 NULL-only uncategorized, D-07 rule action=category, D-15 delete guard — binding)
  </read_first>
  <behavior>
    - Test (categories repo): create returns a row with id+name+color; list returns rows ordered by id (or name); update changes name/color; round-trips through a fresh session.
    - Test (rules repo): create persists predicate as JSONB (`predicate.model_dump(mode="json")`) and re-reads it back through `RulePredicate.model_validate(...)`; `list_active_ordered()` returns rules sorted `priority ASC, id ASC`; reorder rewrites priorities for a given ordered id list in one transaction with no UNIQUE violation.
    - Test (reference_counts): returns `(rules_referencing, transactions_referencing)` — nonzero when a rule or transaction references the category, `(0,0)` otherwise.
  </behavior>
  <action>
Create `src/finance_bro/db/category_repo.py` mirroring `tracked_fx_currency_repo.py`: constructor `def __init__(self, session: AsyncSession): self._s = session`; methods `create(name, color)`, `get(cid)`, `list_all()` (ORM `select(Category).order_by(Category.id)`), `update(cid, name=None, color=None)`, `delete(cid)`, and `reference_counts(category_id) -> tuple[int,int]` using TWO parameterized `text()` counts (`SELECT count(*) FROM rules WHERE category_id=:cid` and `SELECT count(*) FROM transactions WHERE category_id=:cid`) — never f-string a query (D-15 / V5 input validation).
Create `src/finance_bro/db/rule_repo.py`: `create(priority, category_id, predicate_json, description)`, `get(rid)`, `list_all()` and `list_active_ordered()` (`select(Rule).order_by(Rule.priority, Rule.id)` — deterministic tiebreak, Pitfall 6), `update(rid, ...)`, `delete(rid)`, and `reorder(ordered_ids: list[int])` that rewrites `priority` for the sequence in one transaction (use a `text()` UPDATE per id, or a two-phase rewrite to avoid the `uq_rules_priority` UNIQUE collision — e.g. offset priorities into a high range first, then renumber). Store predicate via `model_dump(mode="json")`; the repo accepts/returns the JSON dict (validation lives at the route DTO).
Extend `transaction_repo.py` `ROLLUP_SQL`: add `LEFT JOIN categories c ON c.id = t.category_id` (LEFT — `category_id` may be NULL, D-02) and select `t.category_id, t.category_source, c.name AS category_name, c.color AS category_color`; carry these into the dict rows returned by `list_for_account`.
Extend `schemas.py`: add `CategoryOut(from_attributes=True)` (id, name, color), `CategoryCreateIn`/`CategoryUpdateIn` (name, color — bounded), `RuleOut` (id, priority, category_id, predicate, description), `RuleCreateIn` (priority, category_id, `predicate: RulePredicate`, description) and `RuleReorderIn` (`ordered_ids: list[int]`). Add the four category fields (`category_id`, `category_source`, `category_name`, `category_color`) to `TransactionOut`. Import `RulePredicate` from `finance_bro.categorizer.predicate` so malformed predicates are rejected at request parse (V5).
  </action>
  <verify>
    <automated>uv run pytest tests/test_categories_crud.py tests/test_rules_crud.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `RuleRepo.list_active_ordered()` returns rules sorted by `(priority, id)` (asserted in test_rules_crud).
    - A rule's predicate round-trips: write `RulePredicate(...).model_dump(mode="json")`, read back, `RulePredicate.model_validate(...)` succeeds (asserted in test_rules_crud).
    - `CategoryRepo.reference_counts(cid)` returns `(N_rules, N_tx)` matching seeded/created references.
    - `grep -n "f\"SELECT\|f'SELECT\|f\"UPDATE\|f'UPDATE" src/finance_bro/db/category_repo.py src/finance_bro/db/rule_repo.py` returns nothing (parameterized only).
    - `TransactionOut` exposes `category_id`, `category_name`, `category_color` (no schema validation error when serializing a real row).
  </acceptance_criteria>
  <done>Category + Rule repos and DTOs exist; transactions read carries category data; CRUD round-trip tests green.</done>
</task>

<task type="auto">
  <name>Task 2: routes_categories + routes_rules + main wiring + delete-guard test</name>
  <files>src/finance_bro/api/routes_categories.py, src/finance_bro/api/routes_rules.py, src/finance_bro/api/deps.py, src/finance_bro/main.py, tests/test_category_delete_guard.py</files>
  <read_first>
    - src/finance_bro/api/routes_backfill.py (lines 1-55 — POST + body DTO + HTTPException 4xx idiom)
    - src/finance_bro/api/routes_transactions.py (lines 16-37 — GET + repo call + response_model + Depends(get_session))
    - src/finance_bro/api/deps.py (get_session as-is; get_import_service:49-52 as the provider idiom)
    - src/finance_bro/main.py (lines 39-45 import tuple; lines 138-143 include_router block; lines 23-26 / 95 no-auth + APP_DISABLE_SCHEDULER)
    - .planning/phases/04-categorized-spending/04-RESEARCH.md (§Pattern 6 CRUD + delete guard; §Pattern 1 predicate at boundary)
    - .planning/phases/04-categorized-spending/04-CONTEXT.md (D-15 — 409 lists N rules + M transactions; binding)
  </read_first>
  <action>
Create `src/finance_bro/api/routes_categories.py`: `router = APIRouter()`; `GET /api/categories` (response_model `list[CategoryOut]`), `POST /api/categories` (body `CategoryCreateIn` → CategoryOut, 201), `PATCH /api/categories/{cid}` (body `CategoryUpdateIn` → CategoryOut), `DELETE /api/categories/{cid}` — the D-15 guard: call `CategoryRepo(session).reference_counts(cid)`; if either count is nonzero, `raise HTTPException(status_code=409, detail={"rules": rules_n, "transactions": tx_n})`; else delete (the FK `ON DELETE RESTRICT` from migration 0004 is the backstop if a path forgets). Each handler takes `session: Annotated[AsyncSession, Depends(get_session)]`.
Create `src/finance_bro/api/routes_rules.py`: `GET /api/rules` (response_model `list[RuleOut]`, priority-ordered via `list_active_ordered`), `POST /api/rules` (body `RuleCreateIn` with `predicate: RulePredicate` → RuleOut, 201; Pydantic rejects a malformed predicate at parse → 422), `PATCH /api/rules/{rid}` (update priority/category_id/predicate/description), `DELETE /api/rules/{rid}`, and `PATCH /api/rules/reorder` (body `RuleReorderIn` → reorders priorities in one transaction). Hard-code `/api/...` paths; no prefix, no auth (DEP-02).
`deps.py`: `get_session` is reused as-is; no new provider needed unless a service is injected (repos are instantiated per-handler from the session — mirror routes_transactions). Do NOT add middleware.
`main.py`: add `routes_categories, routes_rules` to the `from finance_bro.api import (...)` tuple and add `app.include_router(routes_categories.router)` + `app.include_router(routes_rules.router)` to the include block.
Create `tests/test_category_delete_guard.py` (integration, `client` fixture, `APP_DISABLE_SCHEDULER=1`): seed a category + a rule referencing it (or a transaction with that category_id), `DELETE /api/categories/{cid}` → assert 200/204 status is NOT returned; assert `409` with a body carrying `rules` and `transactions` counts. Also assert deleting an unreferenced category succeeds. Add a quick CRUD smoke for both routers if not already covered by Task 1's repo tests (create category via POST, create rule via POST with the canonical ATB predicate, list both).
  </action>
  <verify>
    <automated>uv run pytest tests/test_category_delete_guard.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_category_delete_guard.py -x` exits 0.
    - `DELETE /api/categories/{cid}` on a referenced category returns HTTP 409 with `detail` containing `rules` and `transactions` integer counts (asserted in test).
    - `POST /api/rules` with a malformed predicate (e.g. unknown `op`) returns HTTP 422 (Pydantic boundary rejection — assert in test).
    - `POST /api/rules` with the canonical ATB predicate `{"all":[{"op":"in_int","field":"mcc","values":[5411,5499]},{"op":"amount_sign","sign":"debit"},{"op":"icontains","field":"description","value":"ATB"}]}` returns 201.
    - `grep -n "routes_categories\|routes_rules" src/finance_bro/main.py` shows both in the import tuple AND the include block.
  </acceptance_criteria>
  <done>Both routers mounted; category delete returns 409 on reference; malformed predicate rejected at boundary; delete-guard test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → rule/category CRUD | LAN/Tailscale caller submits rule predicate JSON + category mutations; untrusted at the app boundary. |
| API → Postgres | Rule/category writes and the category-delete reference check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-validate | Tampering / injection | routes_rules.py (predicate body) | mitigate | `RulePredicate` Pydantic discriminated union validates the predicate at request parse; an unknown `op` or malformed shape → 422 before the interpreter ever runs (V5 input validation, D-05). |
| T-4-sqli | Tampering | category_repo.py / rule_repo.py | mitigate | All reads/writes use ORM or `text()` with bound params; no f-string SQL (existing convention). Acceptance grep enforces no f-string SELECT/UPDATE. |
| T-4-fk | Tampering / referential corruption | routes_categories.py DELETE | mitigate | `reference_counts` pre-check → 409 with counts; FK `ON DELETE RESTRICT` (migration 0004) is the DB backstop (D-15). `tests/test_category_delete_guard.py` proves it. |
| T-4-SC | Tampering | npm/pip/uv installs | accept | Zero new packages in Phase 4 (RESEARCH §Package Legitimacy Audit). No install task. |
</threat_model>

<verification>
Run `uv run pytest tests/test_categories_crud.py tests/test_rules_crud.py tests/test_category_delete_guard.py -x -q`, then full suite `uv run pytest -q` green. Confirm both routers are reachable (the delete-guard test exercises the `client` fixture end-to-end).
</verification>

<success_criteria>
- Categories and rules are fully CRUD-able over HTTP; rule lists are priority-ordered; priorities are reorderable.
- A malformed predicate is rejected at the API boundary (422); a referenced category delete returns 409 with counts.
- The transactions read path carries category id/name/color.
- Three integration tests green; full suite green.
</success_criteria>

<output>
Create `.planning/phases/04-categorized-spending/04-02-SUMMARY.md` when done.
</output>
