---
phase: 04-categorized-spending
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/finance_bro/db/models.py
  - alembic/versions/0004_categorized_spending.py
  - src/finance_bro/categorizer/__init__.py
  - src/finance_bro/categorizer/predicate.py
  - src/finance_bro/categorizer/fields.py
  - src/finance_bro/categorizer/interpreter.py
  - src/finance_bro/categorizer/engine.py
  - tests/test_categorizer_interpreter.py
  - tests/test_no_eval_in_categorizer.py
  - tests/test_field_resolver.py
  - tests/test_engine_first_match.py
  - tests/test_migration_0004.py
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03, CAT-04]
must_haves:
  truths:
    - "Migration 0004 applies cleanly and seeds ~15 categories plus the MCC default rules"
    - "transactions.category_id has a FK to categories.id with ON DELETE RESTRICT"
    - "The pure categorizer engine matches the canonical ATB example and returns first-match-wins category id"
    - "is_user_locked rows are refused by the engine (return _SKIP sentinel)"
    - "No eval/exec/regex is reachable anywhere in categorizer/"
  artifacts:
    - path: "alembic/versions/0004_categorized_spending.py"
      provides: "categories + rules tables, transactions.category_id FK, taxonomy + MCC seed"
      contains: "create_table"
    - path: "src/finance_bro/categorizer/predicate.py"
      provides: "Pydantic discriminated-union predicate AST (closed op vocabulary)"
      contains: "discriminator"
    - path: "src/finance_bro/categorizer/interpreter.py"
      provides: "match-based op evaluator, no eval/regex"
      contains: "match"
    - path: "src/finance_bro/categorizer/engine.py"
      provides: "first-match-wins categorize_row / categorize_rows over priority-ordered rules"
      exports: ["categorize_row", "categorize_rows"]
    - path: "src/finance_bro/db/models.py"
      provides: "Category + Rule ORM models; FK arg on Transaction.category_id"
      contains: "class Category"
  key_links:
    - from: "src/finance_bro/categorizer/engine.py"
      to: "src/finance_bro/categorizer/interpreter.py"
      via: "eval_condition over each predicate condition"
      pattern: "eval_condition"
    - from: "src/finance_bro/categorizer/interpreter.py"
      to: "src/finance_bro/categorizer/fields.py"
      via: "RowView field resolution"
      pattern: "row\\.(text|int_field|str_field|amount_minor|hold)"
---

<objective>
Build the shared foundation for Phase 4: migration 0004 (categories + rules tables, the `transactions.category_id` FK ON DELETE RESTRICT, and the seeded ~15-category taxonomy + MCC default rules), the Category/Rule ORM models, and the pure `categorizer/` package (predicate AST + field resolver + match-based interpreter + first-match-wins engine). This is the slice both later slices (auto-categorize-on-import D-11, run-over-history CAT-05) reuse verbatim.

Purpose: Establish the single categorization mechanism (the rules engine — D-04) and the schema it runs against, with the eval-free / lock-respecting / first-match-wins invariants proven by pure unit tests before any service wiring exists.

Output: Applied migration 0004, ORM models, the pure categorizer package, and 5 green test files (`test_categorizer_interpreter.py`, `test_no_eval_in_categorizer.py`, `test_field_resolver.py`, `test_engine_first_match.py`, `test_migration_0004.py`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-categorized-spending/04-CONTEXT.md
@.planning/phases/04-categorized-spending/04-RESEARCH.md
@.planning/phases/04-categorized-spending/04-PATTERNS.md
@.planning/phases/04-categorized-spending/04-VALIDATION.md

<interfaces>
<!-- Existing Transaction columns the engine reads (from src/finance_bro/db/models.py:46-83): -->
<!-- category_id: BigInteger nullable, NO FK yet (this plan adds it) -->
<!-- category_source: Text nullable; is_user_locked: Boolean server_default false -->
<!-- mcc: Integer nullable; currency: CHAR(3); amount_minor: BigInteger; hold: Boolean; raw_payload: JSONB -->
<!-- raw_payload Mono field names (verified, statement_two_items.json): originalMcc, comment, counterIban, counterEdrpou -->

<!-- Migration idiom (from alembic/versions/0003_fx_truth.py): -->
<!-- revision="0004", down_revision="0003"; op.create_table / op.create_index / op.execute("INSERT ...") seed -->
<!-- Postgres DDL is transactional — whole revision runs in one transaction. -->

<!-- raw_payload .get() safety idiom (transaction_repo.py:49-59 _op_currency_alpha): -->
<!-- raw_payload.get("currencyCode") returns None on absent key, never KeyError. Mirror for fields.py. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0004 + Category/Rule ORM models + Wave-0 migration test</name>
  <files>alembic/versions/0004_categorized_spending.py, src/finance_bro/db/models.py, tests/test_migration_0004.py</files>
  <read_first>
    - alembic/versions/0003_fx_truth.py (EXACT migration template: revision header, create_table, create_index, op.execute seed, transactional downgrade)
    - src/finance_bro/db/models.py (lines 46-83 Transaction; lines 136-179 FxRate/TrackedFxCurrency seed-table shape to copy; ForeignKey ondelete="RESTRICT" idiom at lines 50-54)
    - tests/test_fx_repos.py (session_factory integration-test fixture pattern)
    - .planning/phases/04-categorized-spending/04-RESEARCH.md (§Code Examples migration 0004; §Default Taxonomy; §Default MCC Seed Rules; §Runtime State Inventory — verify category_id all-NULL before FK)
    - .planning/phases/04-categorized-spending/04-CONTEXT.md (D-01, D-03, D-04, D-15 — binding)
  </read_first>
  <action>
Create `alembic/versions/0004_categorized_spending.py` with `revision="0004"`, `down_revision="0003"`, mirroring the 0003 idiom. In `upgrade()`:
(1) `op.create_table("categories", ...)`: columns `id` BigInteger PK autoincrement, `name` Text NOT NULL, `color` Text nullable, `created_at` TIMESTAMP(tz) NOT NULL server_default `now()`; `UniqueConstraint("name", name="uq_categories_name")`.
(2) `op.create_table("rules", ...)`: `id` BigInteger PK autoincrement, `priority` Integer NOT NULL, `category_id` BigInteger `ForeignKey("categories.id", ondelete="RESTRICT")` NOT NULL, `predicate` postgresql.JSONB NOT NULL, `description` Text nullable (human label — NOT a predicate field, per D-07), `created_at` TIMESTAMP(tz) NOT NULL server_default `now()`; `UniqueConstraint("priority", name="uq_rules_priority")` (Pitfall 6 — forbid priority ties).
(3) `op.create_index("ix_rules_priority", "rules", ["priority"])`.
(4) `op.create_foreign_key("fk_transactions_category", "transactions", "categories", ["category_id"], ["id"], ondelete="RESTRICT")` (D-03/D-15 — FK on the pre-existing all-NULL column; safe).
(5) Seed the ~15-category taxonomy via `op.execute("INSERT INTO categories (name, color) VALUES ...")` using the RESEARCH §Default Taxonomy table (Groceries, Cafe & Restaurants, Transport, Fuel, Utilities, Health & Pharmacy, Shopping, Entertainment, Communications, Cash & ATM, Fees & Commissions, Income, Transfers, Travel, Other/Misc — names/colors are Claude's discretion per D-01).
(6) Seed MCC default RULES via `op.execute("INSERT INTO rules (priority, category_id, predicate, description) VALUES ...")` using the RESEARCH §Default MCC Seed Rules table; each predicate is a JSON literal of shape `{"all":[{"op":"in_int","field":"mcc","values":[...]},{"op":"amount_sign","sign":"debit"}]}`; resolve `category_id` via a subselect `(SELECT id FROM categories WHERE name='Groceries')` so seed order is robust. Assign sequential `priority` values (e.g. 100, 200, 300...) leaving gaps for user rules. NO `MCC_MAP` Python constant anywhere — MCC coverage is seeded rows only (D-04 / Pitfall 3).
`downgrade()`: reverse order — drop FK `fk_transactions_category`, drop index `ix_rules_priority`, drop table `rules`, drop table `categories`.
In `src/finance_bro/db/models.py`: add `class Category(Base)` (`__tablename__="categories"`, mirror TrackedFxCurrency shape: id/name/color/created_at + UniqueConstraint on name) and `class Rule(Base)` (id/priority/category_id with `ForeignKey("categories.id", ondelete="RESTRICT")`/predicate JSONB/description/created_at + UniqueConstraint on priority). Add the `ForeignKey("categories.id", ondelete="RESTRICT")` arg to the existing `Transaction.category_id` mapped_column (currently a bare BigInteger). All needed imports already present (models.py:5-21) — do NOT add new imports.
Create `tests/test_migration_0004.py` (integration, session_factory fixture): assert seeded category count == the seed count you used (~15), assert seeded MCC-rule count == the number of seed rules, assert the FK `fk_transactions_category` exists (query `information_schema.table_constraints` / `pg_constraint`), and assert a clean downgrade-then-upgrade round-trip (or at minimum that the constraint and tables exist after `alembic upgrade head`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_migration_0004.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` applies 0004 with no error (run inside the test container path).
    - `uv run pytest tests/test_migration_0004.py -x` exits 0.
    - `SELECT count(*) FROM categories` equals the seeded taxonomy count (~15) and `SELECT count(*) FROM rules` equals the seeded MCC-rule count.
    - FK constraint named `fk_transactions_category` is present on `transactions.category_id` with `RESTRICT` delete rule.
    - `grep -rn "MCC_MAP\|MCC_TO_CATEGORY" src/finance_bro/` returns nothing (MCC coverage is seeded rules, not a code dict — D-04).
  </acceptance_criteria>
  <done>Migration 0004 applies and seeds taxonomy + MCC rules; FK present; Category/Rule ORM models exist; test_migration_0004 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure categorizer package — predicate AST, field resolver, interpreter, engine</name>
  <files>src/finance_bro/categorizer/__init__.py, src/finance_bro/categorizer/predicate.py, src/finance_bro/categorizer/fields.py, src/finance_bro/categorizer/interpreter.py, src/finance_bro/categorizer/engine.py, tests/test_categorizer_interpreter.py, tests/test_no_eval_in_categorizer.py, tests/test_field_resolver.py, tests/test_engine_first_match.py</files>
  <read_first>
    - .planning/phases/04-categorized-spending/04-RESEARCH.md (§Pattern 1 predicate AST; §Pattern 2 interpreter; §Pattern 3 engine; §Pattern 4 field resolver — the proposed shapes are Claude's discretion to lock)
    - .planning/phases/04-categorized-spending/04-CONTEXT.md (D-05 closed op vocabulary, D-06 flat AND-only, D-07 action=category-only, D-08 column-vs-raw_payload, D-09 lock skip — binding)
    - src/finance_bro/db/transaction_repo.py (lines 49-59 _op_currency_alpha — the raw_payload.get() never-raise discipline to mirror in fields.py)
    - src/finance_bro/services/fx_rollup.py (the pure, no-DB module shape to mirror — no session imports anywhere in categorizer/)
    - src/finance_bro/api/schemas.py (Pydantic BaseModel/Field/ConfigDict idiom to mirror for the predicate AST)
    - CLAUDE.md (§Money — amount_minor is integer minor units; all amount predicates are integer comparisons, never float)
  </read_first>
  <behavior>
    - Test (interpreter): each op evaluates correctly — `icontains` (case-insensitive substring), `equals`, `in_int` (mcc/original_mcc/account_id), `in_str` (currency), `amount_sign` (debit => amount_minor<0, credit => >0), `amount_range` (inclusive min/max integer bounds), `hold_is` (bool). Canonical example `RulePredicate(all=[InInt(field="mcc", values=[5411,5499]), AmountSign(sign="debit"), IContains(field="description", value="ATB")])` matches a row with mcc=5411, amount_minor=-1500, description="ATB Market" and does NOT match the same row with description="Silpo".
    - Test (field resolver): `mcc`/`amount_minor`/`currency`/`hold`/`account_id` resolve from columns; `original_mcc`/`comment`/`counter_iban`/`counter_edrpou` resolve from raw_payload via `.get()`; a row whose raw_payload lacks `counterIban` returns None for `counter_iban` and a condition over it evaluates False (no match), never KeyError.
    - Test (engine): rules evaluated in `priority ASC, id ASC` order; first fully-matching rule wins; a row with `is_user_locked=True` returns the `_SKIP` sentinel (never a category id, never None-as-categorize); a row matching no rule returns None (NULL category — D-02).
    - Test (no-eval guard): no `eval(`/`exec(`/`re.compile`/`import re`/`from re ` appears in any file under `src/finance_bro/categorizer/`.
  </behavior>
  <action>
Create `src/finance_bro/categorizer/` as a PURE package (NO SQLAlchemy/session/DB imports anywhere — mirror `services/fx_rollup.py`).
`predicate.py`: the discriminated-union AST per RESEARCH §Pattern 1 — condition models `IContains`/`Equals`/`InInt`/`InStr`/`AmountSign`/`AmountRange`/`HoldIs` each with a `Literal` `op` tag; `Condition = Annotated[Union[...], Field(discriminator="op")]`; `RulePredicate(BaseModel)` with `all: list[Condition] = Field(min_length=1)` (flat AND-only — D-06). Field enums encode the closed vocabulary: TextField over `description/comment/counter_iban/counter_edrpou`, IntSetField over `mcc/original_mcc/account_id`, StrSetField over `currency`. Amount fields are `int | None` minor units only (CLAUDE.md §Money) — never float.
`fields.py`: a `RowView` adapter (a small dataclass/class constructed from a transaction's columns + raw_payload dict) exposing `text(field)`, `int_field(field)`, `str_field(field)`, `.amount_minor`, `.hold`, `.is_user_locked`, `.id`, `.category_id`. Column-backed fields (`description`, `mcc`, `account_id`, `currency`, `hold`, `amount_minor`) read attributes; raw_payload-backed fields (`comment`→`comment`, `counter_iban`→`counterIban`, `counter_edrpou`→`counterEdrpou`, `original_mcc`→`originalMcc`) read `raw_payload.get(...)` and return None on absence (mirror `_op_currency_alpha` — D-08 / Pitfall 5). Provide a module-level `make_row(...)` helper for pure tests.
`interpreter.py`: `eval_condition(cond, row) -> bool` as a `match` on the discriminated union per RESEARCH §Pattern 2. A None field value yields False (no match), never an exception. There is NO string→code path — the `op` tag selects a pre-written branch; values are only compared (D-05 / Anti-pattern 8). Do NOT import `re`, do NOT call `eval`/`exec`.
`engine.py`: a `CompiledRule` (priority, id, category_id, predicate: RulePredicate) and `categorize_row(row, rules) -> int | None | _SKIP` per RESEARCH §Pattern 3 — return `_SKIP` sentinel immediately if `row.is_user_locked` (D-09 defense-in-depth), else iterate pre-sorted rules and return the first rule's `category_id` whose `all(eval_condition(c, row) for c in rule.predicate.all)` is True, else return None (D-02). Add `categorize_rows(rows, rules) -> list[(row_id, category_id)]` that the import-step (Plan 03) and history sweep (Plan 04) both call verbatim — skipping `_SKIP` rows from the output. Whether engine is a module of functions or a small class is Claude's discretion as long as both consumers reuse it (D-11).
Create the four pure test files (`test_categorizer_interpreter.py`, `test_field_resolver.py`, `test_engine_first_match.py`, `test_no_eval_in_categorizer.py`) per the `<behavior>` block — plain pytest, no Postgres/conftest fixtures, using `make_row(...)`. The no-eval guard reads each `*.py` under `categorizer/` and asserts none of `eval(`/`exec(`/`re.compile`/`import re`/`from re ` appear.
  </action>
  <verify>
    <automated>uv run pytest tests/test_categorizer_interpreter.py tests/test_field_resolver.py tests/test_engine_first_match.py tests/test_no_eval_in_categorizer.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_categorizer_interpreter.py tests/test_field_resolver.py tests/test_engine_first_match.py tests/test_no_eval_in_categorizer.py -x` exits 0.
    - The canonical predicate `all=[in_int mcc [5411,5499], amount_sign debit, icontains description "ATB"]` matches an ATB debit row and rejects a Silpo row (asserted in test_categorizer_interpreter).
    - A locked row (`is_user_locked=True`) yields the `_SKIP` sentinel from `categorize_row` (asserted in test_engine_first_match).
    - `grep -rEn "eval\(|exec\(|re\.compile|^import re|^from re " src/finance_bro/categorizer/` returns nothing.
    - `grep -rn "import.*sqlalchemy\|AsyncSession\|session" src/finance_bro/categorizer/` returns nothing (package is pure — D-11).
  </acceptance_criteria>
  <done>Pure categorizer package exists with eval-free interpreter, lock-skipping first-match-wins engine, and absent-key-safe field resolver; four pure unit test files green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored predicate JSON → interpreter | A rule's predicate JSON (authored via API in Plan 02) crosses into evaluation here; must never reach an eval/exec path. |
| raw_payload (Mono-sourced JSON) → field resolver | Untrusted external JSON; absent/typed-wrong keys must not crash evaluation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-eval | Elevation of Privilege / Tampering | categorizer/interpreter.py | mitigate | Closed-op `match` interpreter; no `eval`/`exec`/`re` reachable. `tests/test_no_eval_in_categorizer.py` static guard enforces absence (D-05 / Anti-pattern 8). |
| T-4-payload | Denial of Service / data integrity | categorizer/fields.py | mitigate | Field resolver uses `raw_payload.get()` → None on absent key; condition over None evaluates False, never KeyError (D-08 / Pitfall 5). `tests/test_field_resolver.py` proves it. |
| T-4-lock | Tampering / lost-update | categorizer/engine.py | mitigate | Engine returns `_SKIP` for `is_user_locked` rows as defense-in-depth (D-09); proven by `tests/test_engine_first_match.py`. Query-level filter lands in Plans 03/04. |
| T-4-fk | Tampering / referential corruption | migration 0004 / models.py | mitigate | `transactions.category_id` and `rules.category_id` FKs use `ON DELETE RESTRICT` (D-03/D-15); migration test asserts FK presence. App pre-check 409 lands in Plan 02. |
| T-4-SC | Tampering | npm/pip/uv installs | accept | Phase 4 installs zero new packages (RESEARCH §Package Legitimacy Audit — vacuously satisfied); no install task in this plan. |
</threat_model>

<verification>
Run the foundation suite: `uv run pytest tests/test_migration_0004.py tests/test_categorizer_interpreter.py tests/test_field_resolver.py tests/test_engine_first_match.py tests/test_no_eval_in_categorizer.py -x -q`. Then full suite `uv run pytest -q` must remain green (Phases 1-3 unaffected — purely additive DDL + new pure package).
</verification>

<success_criteria>
- Migration 0004 applies, seeds ~15 categories + MCC default rules, and adds the `transactions.category_id` FK (RESTRICT).
- The pure categorizer engine matches the canonical ATB example, is first-match-wins and deterministic, refuses locked rows, and contains no eval/exec/regex.
- Five test files green; full suite green.
</success_criteria>

<output>
Create `.planning/phases/04-categorized-spending/04-01-SUMMARY.md` when done.
</output>
