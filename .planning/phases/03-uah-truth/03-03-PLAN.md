---
phase: 03-uah-truth
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/finance_bro/services/fx_rollup.py
  - src/finance_bro/db/transaction_repo.py
  - src/finance_bro/importers/monobank.py
  - src/finance_bro/api/schemas.py
  - src/finance_bro/api/routes_transactions.py
autonomous: true
requirements: [FX-03, FX-04]
must_haves:
  truths:
    - "GET /api/transactions returns a UAH equivalent for a foreign-currency row, computed on read via the LATERAL join"
    - "A Sunday-dated USD transaction rolls up using Friday's NBU rate; fx_rate_date and fx_source are visible in the response"
    - "An FX-on-card row (account currency != operation currency) uses account-amount × NBU rate, never double-converted; fx_source = mono_card"
    - "When no rate is available, the row still appears with null FX fields and fx_stale=true"
    - "The Mono importer populates attributed_day on insert and it is frozen on hold→cleared upsert"
  artifacts:
    - path: "src/finance_bro/services/fx_rollup.py"
      provides: "Decimal UAH rollup math + fx_source/fx_stale classification (D-11..D-14)"
      contains: "ROUND_HALF_EVEN"
    - path: "src/finance_bro/db/transaction_repo.py"
      provides: "LATERAL-join read + attributed_day frozen-by-omission upsert"
      contains: "LEFT JOIN LATERAL"
    - path: "src/finance_bro/api/schemas.py"
      provides: "TransactionOut + 5 FX fields"
      contains: "fx_source"
  key_links:
    - from: "src/finance_bro/db/transaction_repo.py"
      to: "fx_rates"
      via: "LEFT JOIN LATERAL on (currency, rate_date <= attributed_day)"
      pattern: "rate_date <= t.attributed_day"
    - from: "src/finance_bro/api/routes_transactions.py"
      to: "src/finance_bro/services/fx_rollup.py"
      via: "rollup() per row → TransactionOut"
      pattern: "rollup\\("
    - from: "src/finance_bro/importers/monobank.py"
      to: "transactions.attributed_day"
      via: "time.astimezone(Kyiv).date() on CanonicalTransaction"
      pattern: "Europe/Kyiv"
---

<objective>
Deliver the read slice: every foreign-currency transaction shows an honest UAH equivalent computed on read (FX-03), FX-on-card rows use the account-currency amount without double-conversion (FX-04), and the Mono importer freezes a Kyiv `attributed_day` on first write (D-09). After this plan, `GET /api/transactions` returns the five FX fields and the Sunday→Friday fallback works.

Purpose: FX-03 (compute on read via LATERAL, never denormalized) and FX-04 (no double-conversion) are the user-facing payoff of the phase — "look at any USD/EUR transaction and see an honest UAH equivalent". This plan reads the `fx_rates` the importer/bootstrap populate.

Output: `fx_rollup.py` math helper, the LATERAL-join read + frozen-by-omission attributed_day upsert in `transaction_repo.py`, the importer's attributed_day population, the 5 new `TransactionOut` fields, and the route wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-uah-truth/03-CONTEXT.md
@.planning/phases/03-uah-truth/03-RESEARCH.md
@.planning/phases/03-uah-truth/03-PATTERNS.md
@.planning/phases/03-uah-truth/03-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor builds against. Use directly — no exploration. -->

D-14 LATERAL SQL (verbatim, locked — RESEARCH Pattern 1 Approach A `text()`):
```sql
SELECT t.id, t.account_id, t.source_tx_id, t.amount_minor, t.currency,
       t.time, t.hold, t.raw_payload, t.attributed_day,
       fx.rate AS fx_rate, fx.rate_date AS fx_rate_date
FROM transactions t
LEFT JOIN LATERAL (
    SELECT rate, rate_date FROM fx_rates
    WHERE currency = t.currency AND rate_date <= t.attributed_day
    ORDER BY rate_date DESC LIMIT 1
) fx ON true
WHERE t.account_id = :account_id AND NOT t.is_deleted
ORDER BY t.time DESC
```
Execute via `(await session.execute(ROLLUP_SQL, {"account_id": account_id})).mappings().all()`.

D-14 rollup math (verbatim, locked — RESEARCH Code Examples):
```python
from decimal import Decimal, ROUND_HALF_EVEN
major = (Decimal(amount_minor) / 100) * fx_rate
uah_minor = int(major.quantize(Decimal("0.01"), ROUND_HALF_EVEN) * 100)
```
fx_source (D-11, audit-only label; math identical for mono_card/nbu):
`source = "mono_card" if (op_currency_alpha and op_currency_alpha != currency) else "nbu"`

From src/finance_bro/api/schemas.py (TransactionOut, lines 34-45) — add 5 fields (D-10):
```python
uah_amount_minor: int | None = None
fx_rate: str | None = None          # Decimal-as-string ("28.34250000"); never float
fx_rate_date: date | None = None
fx_source: Literal["native_uah", "mono_card", "nbu"]
fx_stale: bool
```
Add `from datetime import date` and `from typing import Literal`. Update the file docstring: `fx_rate` is the deliberate string-transport exception to "money is always int".

From src/finance_bro/db/transaction_repo.py:
- `insert_many` (lines 46-72): add `"attributed_day": t.attributed_day` to the INSERT `rows` dict; LEAVE IT OUT of `set_={...}` (lines 68-71 stay exactly three columns: hold, amount_minor, raw_payload) — frozen-by-omission (D-09).
- `list_for_account` (lines 83-96): replace with the LATERAL `text()` query above.

From src/finance_bro/importers/base.py (after Plan 03-02): `CanonicalTransaction` gains nothing new structurally, but the importer must set `attributed_day`. If CanonicalTransaction has no attributed_day field yet, add it as an optional defaulted field (`attributed_day: date | None = None`) consistent with description/mcc.

From src/finance_bro/importers/monobank.py (fetch_statement yield, lines 127-137):
- existing `occurred_at=datetime.fromtimestamp(item["time"], tz=UTC)`; derive attributed_day from the SAME source:
  `attributed_day=datetime.fromtimestamp(item["time"], tz=UTC).astimezone(ZoneInfo("Europe/Kyiv")).date()`.
- raw_payload already carries `currencyCode` (numeric) — the rollup reads it at read time; no new importer field for fx_source.

From src/finance_bro/api/routes_transactions.py (lines 22-30): keep the "no card → empty list" guard; map each joined row through `fx_rollup.rollup(...)` then into TransactionOut. op-currency for the rollup = `numeric_to_alpha(raw_payload["currencyCode"])` with a defensive fallback to `nbu` when currencyCode is absent or unmapped (D-11).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the fx_rollup Decimal math + classification helper</name>
  <files>src/finance_bro/services/fx_rollup.py</files>
  <read_first>
    - src/finance_bro/services/import_service.py (analog for @dataclass(frozen=True) result + module docstring style)
    - src/finance_bro/importers/currency_map.py (numeric_to_alpha for the op-currency lookup at call site)
    - .planning/phases/03-uah-truth/03-PATTERNS.md section "src/finance_bro/services/fx_rollup.py" + "No Analog Found" (use RESEARCH Code Examples verbatim)
    - .planning/phases/03-uah-truth/03-RESEARCH.md section "Code Examples → UAH rollup math" + Pitfall 1, Pitfall 2
    - .planning/phases/03-uah-truth/03-CONTEXT.md D-11, D-12, D-13, D-14
    - CLAUDE.md section "Money / Decimal Handling" (getcontext().prec = 28, ROUND_HALF_EVEN, Decimal-as-string transport)
  </read_first>
  <behavior>
    - test_fx_rollup_math.py: rollup of a known amount_minor + rate matches the hand-computed banker's-rounded kopeck value.
    - A `mono_card` row and an `nbu` row with identical account amount_minor + same fx_rate + same day produce IDENTICAL uah_amount_minor (FX-04 no-double-conversion property).
    - native_uah: currency=="UAH" → uah_amount_minor==amount_minor, fx_rate=="1.00000000", fx_source=="native_uah", fx_stale==False.
    - No rate (fx_rate is None): all FX value fields None, fx_stale==True (D-12).
    - fx_stale==True iff fx_rate_date < attributed_day OR no rate; False when equal or native_uah (D-13).
  </behavior>
  <action>
    Create `src/finance_bro/services/fx_rollup.py` with a frozen result dataclass `FxFields(uah_amount_minor: int | None, fx_rate: str | None, fx_rate_date: date | None, fx_source: Literal["native_uah","mono_card","nbu"], fx_stale: bool)` and a pure function `rollup(amount_minor: int, currency: str, fx_rate: Decimal | None, fx_rate_date: date | None, attributed_day: date, op_currency_alpha: str | None) -> FxFields`. Implement exactly per D-11..D-14:
    - if `currency == "UAH"`: return `FxFields(amount_minor, "1.00000000", attributed_day, "native_uah", False)` (D-11 native_uah).
    - `source = "mono_card" if (op_currency_alpha and op_currency_alpha != currency) else "nbu"` (D-11 audit label).
    - if `fx_rate is None`: return `FxFields(None, None, None, source, fx_stale=True)` (D-12).
    - else: `major = (Decimal(amount_minor) / 100) * fx_rate`; `uah_minor = int(major.quantize(Decimal("0.01"), ROUND_HALF_EVEN) * 100)`; `stale = fx_rate_date < attributed_day` (D-13); `fx_rate` serialized as `f"{fx_rate:.8f}"` (Decimal-as-string, never float). Return `FxFields(uah_minor, f"{fx_rate:.8f}", fx_rate_date, source, fx_stale=stale)`.
    The math for `mono_card` and `nbu` is IDENTICAL (`amount_minor × NBU_rate`) — never triangulate via operationAmount (FX-04 / Pitfall 8). Verify `getcontext().prec = 28` is set at app import (check `core/` or `domain/money.py`); if not set project-wide, set it in this module.
  </action>
  <verify>
    <automated>uv run pytest tests/test_fx_rollup_math.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fx_rollup_math.py` passes, including the mono_card-vs-nbu identical-output property.
    - `grep -c 'ROUND_HALF_EVEN' src/finance_bro/services/fx_rollup.py` >= 1.
    - `grep -c 'float(' src/finance_bro/services/fx_rollup.py` == 0 (no float on the money path).
    - `rollup(..., currency="UAH", ...)` returns `fx_source == "native_uah"` and `fx_rate == "1.00000000"`.
    - `uv run ruff check src/finance_bro/services/fx_rollup.py` and `uv run basedpyright src/finance_bro/services/fx_rollup.py` pass.
  </acceptance_criteria>
  <done>rollup() encodes D-11..D-14 with banker's rounding and Decimal-as-string transport; the no-double-conversion property holds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LATERAL-join read + frozen attributed_day upsert + importer attributed_day</name>
  <files>src/finance_bro/db/transaction_repo.py, src/finance_bro/importers/monobank.py, src/finance_bro/importers/base.py</files>
  <read_first>
    - src/finance_bro/db/transaction_repo.py (file being modified — insert_many lines 46-81, list_for_account lines 83-96, docstring lines 1-11)
    - src/finance_bro/importers/monobank.py (fetch_statement yield, lines 127-137 — occurred_at derivation to mirror)
    - src/finance_bro/importers/base.py (CanonicalTransaction — add attributed_day field if absent)
    - .planning/phases/03-uah-truth/03-PATTERNS.md sections "src/finance_bro/db/transaction_repo.py" and "src/finance_bro/importers/monobank.py" (verbatim ROLLUP_SQL + frozen-by-omission + attributed_day snippet)
    - .planning/phases/03-uah-truth/03-RESEARCH.md section "Pattern 1 → Approach A" + "Pattern 3: attributed_day frozen-on-first-write" + "Code Examples → attributed_day on the importer boundary"
    - .planning/phases/03-uah-truth/03-CONTEXT.md D-09, D-14
  </read_first>
  <behavior>
    - test_fx_rollup_join.py: with fx_rates seeded ONLY with the Friday 2026-05-08 USD row and a USD tx attributed to Sunday 2026-05-10, list_for_account returns the row joined with fx_rate_date == 2026-05-08.
    - test_fx_stale_fallback.py: with no fx_rates rows, the USD tx still appears (LEFT JOIN), fx_rate/fx_rate_date come back NULL.
    - hold→cleared upsert does NOT mutate attributed_day (frozen by omission) even when the cleared row's time changes.
    - The Mono importer sets attributed_day = time→Kyiv date on insert.
  </behavior>
  <action>
    In transaction_repo.py:
    (1) In `insert_many`, add `"attributed_day": t.attributed_day` to each row dict (alongside description/mcc/hold). Leave the `set_={...}` clause EXACTLY the existing three columns (hold, amount_minor, raw_payload) — attributed_day is frozen by omission (D-09 / D-10 invariant; the docstring already names it in the frozen list).
    (2) Replace `list_for_account` with a module-level `ROLLUP_SQL = text("""...""")` containing the verbatim D-14 LATERAL query (see interfaces), executed with `.mappings().all()`. Return the list of mapping rows (each carrying joined `fx_rate` and `fx_rate_date` alongside the transaction columns). The route consumes these; do NOT compute the rollup in the repo — the repo returns raw joined rows, the rollup math lives in fx_rollup.py (Plan 03-03 Task 1).
    In base.py: if `CanonicalTransaction` lacks an `attributed_day` field, add `attributed_day: date | None = None` (defaulted, consistent with description/mcc; add `from datetime import date` if needed).
    In monobank.py: in the `fetch_statement` yield, add `attributed_day=datetime.fromtimestamp(item["time"], tz=UTC).astimezone(ZoneInfo("Europe/Kyiv")).date()` to the `CanonicalTransaction(...)` construction; add `from zoneinfo import ZoneInfo` (and `KYIV = ZoneInfo("Europe/Kyiv")` module constant if matching the existing style). Derive from the same `item["time"]` source as `occurred_at`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_fx_rollup_join.py tests/test_fx_stale_fallback.py tests/test_hold_cleared_upsert.py tests/test_idempotency.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fx_rollup_join.py` passes: Sunday tx resolves to Friday's rate via the LATERAL fallback (fx_rate_date == 2026-05-08).
    - `tests/test_fx_stale_fallback.py` passes: no-rate row still returned with NULL joined rate.
    - `tests/test_hold_cleared_upsert.py` and `tests/test_idempotency.py` still pass (frozen-by-omission preserved; attributed_day NOT in the SET clause).
    - `grep -c 'LEFT JOIN LATERAL' src/finance_bro/db/transaction_repo.py` >= 1.
    - The `set_={...}` in `insert_many` contains exactly the keys `hold`, `amount_minor`, `raw_payload` (no `attributed_day`).
    - `grep -c "Europe/Kyiv" src/finance_bro/importers/monobank.py` >= 1.
    - `uv run ruff check src/finance_bro/db/transaction_repo.py src/finance_bro/importers/monobank.py src/finance_bro/importers/base.py` passes.
  </acceptance_criteria>
  <done>Read path uses the LATERAL join with Sunday→Friday fallback; attributed_day is set on insert and frozen on upsert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add 5 FX fields to TransactionOut and wire the route through fx_rollup</name>
  <files>src/finance_bro/api/schemas.py, src/finance_bro/api/routes_transactions.py</files>
  <read_first>
    - src/finance_bro/api/schemas.py (file being modified — TransactionOut lines 34-45, docstring lines 1-8)
    - src/finance_bro/api/routes_transactions.py (file being modified — lines 22-30, the no-card guard)
    - src/finance_bro/services/fx_rollup.py (the rollup() signature from Task 1)
    - src/finance_bro/importers/currency_map.py (numeric_to_alpha for op-currency, with defensive fallback)
    - .planning/phases/03-uah-truth/03-PATTERNS.md sections "src/finance_bro/api/schemas.py" and "src/finance_bro/api/routes_transactions.py"
    - .planning/phases/03-uah-truth/03-CONTEXT.md D-10, D-11, D-12
  </read_first>
  <behavior>
    - test_transactions_route.py (existing) still passes with the 5 new fields present and defaulted.
    - test_fx_on_card.py: an EUR-account row with raw_payload.currencyCode=840 returns fx_source=="mono_card" and uah_amount_minor == EUR amount × NBU EUR/UAH rate (not triangulated).
    - A row whose raw_payload lacks currencyCode (or maps to an unknown numeric) falls back to fx_source=="nbu" without raising (D-11 defensive fallback).
  </behavior>
  <action>
    In schemas.py: add the 5 fields to `TransactionOut` per D-10 (see interfaces): `uah_amount_minor: int | None = None`, `fx_rate: str | None = None`, `fx_rate_date: date | None = None`, `fx_source: Literal["native_uah","mono_card","nbu"]`, `fx_stale: bool`. Add `from datetime import date` and `from typing import Literal`. Update the module docstring to record that `fx_rate` is the deliberate Decimal-as-string exception to the "money is always int minor units" rule.
    In routes_transactions.py: keep the `card is None → []` guard. For each joined mapping row from `list_for_account`, compute `op_currency_alpha` via `numeric_to_alpha(row["raw_payload"].get("currencyCode"))` wrapped so that a missing/unmapped code falls back to `None` (→ fx_source "nbu", per D-11 defensive fallback — do NOT let it raise). Call `fx_rollup.rollup(amount_minor=row["amount_minor"], currency=row["currency"], fx_rate=row["fx_rate"], fx_rate_date=row["fx_rate_date"], attributed_day=row["attributed_day"], op_currency_alpha=op_currency_alpha)`. Build each `TransactionOut` by merging the row's transaction columns with the `FxFields` result (construct directly or via `model_validate` over a merged dict — implementer's call). All five FX fields are computed on read; no DB column change on `transactions` (FX-03).
  </action>
  <verify>
    <automated>uv run pytest tests/test_fx_on_card.py tests/test_transactions_route.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fx_on_card.py` passes: mono_card label + account-currency × NBU math (FX-04 no double-conversion).
    - `tests/test_transactions_route.py` passes with the 5 new fields serialized.
    - `TransactionOut` exposes `uah_amount_minor`, `fx_rate`, `fx_rate_date`, `fx_source`, `fx_stale` (introspect the model fields).
    - `grep -c 'uah_amount_minor' src/finance_bro/db/models.py` == 0 still (computed on read, never denormalized — FX-03).
    - A row with no `currencyCode` in raw_payload does not raise and yields `fx_source == "nbu"`.
    - `uv run ruff check src/finance_bro/api/schemas.py src/finance_bro/api/routes_transactions.py` passes.
  </acceptance_criteria>
  <done>GET /api/transactions returns the 5 computed-on-read FX fields; FX-on-card uses account-currency math; defensive nbu fallback works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fx_rates → rollup math | Joined rate flows into Decimal arithmetic; a float or a wrong currency leg corrupts the UAH total. |
| client → API | Untrusted account_id query crosses into the parameterized LATERAL query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | UAH rollup math | mitigate | Decimal + ROUND_HALF_EVEN quantize to kopeck; property test `test_fx_rollup_math.py`; grep gate asserts no `float(`. |
| T-03-09 | Tampering | FX-on-card double-conversion | mitigate | Math is always `amount_minor × NBU_rate(account_currency)`; `mono_card` is label-only; `test_fx_on_card.py` + math-identity property assert no triangulation (Pitfall 8 / FX-04). |
| T-03-10 | Tampering | denormalized UAH leak | mitigate | FX-03 computed on read; grep gate asserts no `uah_amount_minor` column on transactions. |
| T-03-11 | Injection | LATERAL query account_id | mitigate | Parameterized `text()` bind `:account_id` (no string interpolation); existing route dep validates session scope. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages — RESEARCH § Package Legitimacy Audit confirms zero install surface. |
</threat_model>

<verification>
- `uv run pytest tests/test_fx_rollup_math.py tests/test_fx_rollup_join.py tests/test_fx_stale_fallback.py tests/test_fx_on_card.py tests/test_transactions_route.py tests/test_hold_cleared_upsert.py tests/test_idempotency.py -x` green.
- `grep -c 'uah_amount_minor' src/finance_bro/db/models.py` == 0 (FX-03 immutable).
- `grep -c 'float(' src/finance_bro/services/fx_rollup.py` == 0.
- `uv run ruff check src/finance_bro/services/fx_rollup.py src/finance_bro/db/transaction_repo.py src/finance_bro/importers/monobank.py src/finance_bro/api/schemas.py src/finance_bro/api/routes_transactions.py` passes.
</verification>

<success_criteria>
- USD/EUR transactions show an honest UAH equivalent computed on read (FX-03).
- Sunday→Friday fallback works; fx_rate_date + fx_source visible in the response (SC#5).
- FX-on-card uses account-currency × NBU rate, never double-converted (FX-04).
- attributed_day set on insert, frozen on upsert; no denormalized UAH column.
</success_criteria>

<output>
Create `.planning/phases/03-uah-truth/03-03-SUMMARY.md` when done.
</output>
